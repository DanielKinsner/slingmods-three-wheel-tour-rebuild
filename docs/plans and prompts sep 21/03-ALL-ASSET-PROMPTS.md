# ASSET PROMPT 01 — Race asphalt PBR set (dry + wet)

Generate a seamless 4096×4096 PBR texture set of worn coastal race-circuit asphalt covering a 4 m × 4 m area (≈1024 px/m). Fine dark-grey aggregate 5–15 mm, slightly polished on top, tiny pits, faint oil sheen variation, no large stones, no paint. Provide: baseColor, normal, ORM, height. Then a second 2048 DETAIL normal (0.5 m tile) of micro-aggregate for close camera. Then a WET variant: same layout, darker baseColor (−35% value), roughness 0.05–0.25 in low areas driven by the height map, plus a separate 2048 seamless PUDDLE MASK (greyscale, organic blobs following dips, 20–30% coverage). The current in-game asphalt reads as golf-ball-sized pebbles; this must read correctly from a chase camera 1.2 m above the road at 100 mph.

---
Shared rules for every asset: real-world scale; seamless tiling where stated; PBR metal/rough workflow (baseColor sRGB, normal OpenGL +Y, packed ORM = occlusion R / roughness G / metallic B); no baked lighting or shadows in baseColor; no text, logos or watermarks unless the prompt asks; deliver PNG sources plus KTX2 (Basis UASTC for normals, ETC1S for color) and a one-line README with texel density. Put outputs under public/assets/p11/<asset-name>/ and register them in the asset manifest. Do not overwrite historical assets.
# ASSET PROMPT 02 — Road decal atlas (things that streak under the car)

Generate a 4096×4096 RGBA decal atlas with matching normal+roughness atlases, on transparent background, top-down orthographic, real scale noted per tile: (a) worn white lane line segments, solid and dashed, 15 cm wide, with chipping; (b) yellow edge line; (c) 6 tar-snake crack-seal squiggles, glossy black; (d) 4 expansion seams; (e) 6 tire skid marks: straight lock-ups, curved drift arcs, a pair of launch stripes, and a single fat rear-tire burnout arc (this is a three-wheeler: one wide rear tire); (f) rubbered-in racing-line darkening strip, 3 m wide, soft edges, tileable along length; (g) 4 asphalt repair patches; (h) oil drips and stains; (i) red/white curb paint, tileable; (j) start/finish checker band and grid-slot boxes. Provide a JSON with each tile's UV rect and real-world size in metres.

---
Shared rules for every asset: real-world scale; seamless tiling where stated; PBR metal/rough workflow (baseColor sRGB, normal OpenGL +Y, packed ORM = occlusion R / roughness G / metallic B); no baked lighting or shadows in baseColor; no text, logos or watermarks unless the prompt asks; deliver PNG sources plus KTX2 (Basis UASTC for normals, ETC1S for color) and a one-line README with texel density. Put outputs under public/assets/p11/<asset-name>/ and register them in the asset manifest. Do not overwrite historical assets.
# ASSET PROMPT 03 — Harbor water texture kit

Generate: (1) two seamless 2048×2048 tangent-space water NORMAL maps designed to be scrolled against each other: one with broad 2–4 m swell ripples, one with fine 10–30 cm wind chop; no visible repetition when layered at different scales and directions. (2) A seamless 1024 FOAM texture (greyscale + alpha): lacy shoreline foam, bubbly, not cartoon. (3) A 512 caustics tile for shallow water near the quay. (4) A 256×1 gradient LUT, shallow turquoise (#2fb5a8) → harbor green-blue (#0f5a66) → deep navy (#07222e), for depth-based color. Reference look: Mediterranean marina at golden hour; calm but alive. Also write the GLSL/TSL water material that uses them: dual scrolling normals, Fresnel env reflection, depth-based color + foam at shoreline from scene depth, sun glitter specular, gentle vertex swell, and long vertical light-streak reflections at night.

---
Shared rules for every asset: real-world scale; seamless tiling where stated; PBR metal/rough workflow (baseColor sRGB, normal OpenGL +Y, packed ORM = occlusion R / roughness G / metallic B); no baked lighting or shadows in baseColor; no text, logos or watermarks unless the prompt asks; deliver PNG sources plus KTX2 (Basis UASTC for normals, ETC1S for color) and a one-line README with texel density. Put outputs under public/assets/p11/<asset-name>/ and register them in the asset manifest. Do not overwrite historical assets.
# ASSET PROMPT 04 — Ridge route trees: bark + leaf cards + imposters

Generate for a Southern-California coastal canyon road (coast live oak, sycamore, eucalyptus, plus scrub): (1) three seamless 2048 BARK PBR sets (oak: dark, deeply furrowed; sycamore: pale mottled peeling plates; eucalyptus: smooth ribbons, cream/grey), tiling 1 m wide × 2 m tall; (2) a 4096 LEAF-CARD ATLAS per species: 8–12 branch clusters on transparent background, flat-lit, with matching normal, roughness and a TRANSLUCENCY map (leaves must glow when backlit at golden hour); include a few autumn/dry variants; (3) Blender Python that builds each tree procedurally from these (trunk + 3 branch levels + cards), exports 3 LODs (≈12k / 3k / 600 tris) plus an 8-direction octahedral IMPOSTER atlas (baseColor+normal+depth) for distance; (4) vertex-color channels for wind: R = trunk sway, G = branch, B = leaf flutter. Current trees read as low-detail; target is that a screenshot in the woods at golden hour looks like a driving-sim, not a prototype.

---
Shared rules for every asset: real-world scale; seamless tiling where stated; PBR metal/rough workflow (baseColor sRGB, normal OpenGL +Y, packed ORM = occlusion R / roughness G / metallic B); no baked lighting or shadows in baseColor; no text, logos or watermarks unless the prompt asks; deliver PNG sources plus KTX2 (Basis UASTC for normals, ETC1S for color) and a one-line README with texel density. Put outputs under public/assets/p11/<asset-name>/ and register them in the asset manifest. Do not overwrite historical assets.
# ASSET PROMPT 05 — Forest floor and roadside ground cover

Generate: (1) seamless 4096 PBR ground sets, 4 m tile: dry oak leaf litter over soil; packed canyon dirt with pebbles; dry golden grass field; mossy shaded soil; plus a gravel road-shoulder set. Include height maps for blending. (2) A 4096 RGBA GROUND-COVER ATLAS of cards with normals: fern clumps, tall dry grass tufts (3 heights), wildflowers (sparse, California poppy orange + lupine purple), dead branches, small shrubs. (3) Six low-poly ROCK meshes (boulder to fist-size) with one shared 2048 PBR set, 3 LODs. (4) A shoulder-blend mask so asphalt edges crumble into gravel into dirt into leaf litter rather than meeting the grass at a hard line. Provide instancing density suggestions per metre from the road edge (dense 0–6 m, sparse 6–20 m, imposters beyond).

---
Shared rules for every asset: real-world scale; seamless tiling where stated; PBR metal/rough workflow (baseColor sRGB, normal OpenGL +Y, packed ORM = occlusion R / roughness G / metallic B); no baked lighting or shadows in baseColor; no text, logos or watermarks unless the prompt asks; deliver PNG sources plus KTX2 (Basis UASTC for normals, ETC1S for color) and a one-line README with texel density. Put outputs under public/assets/p11/<asset-name>/ and register them in the asset manifest. Do not overwrite historical assets.
# ASSET PROMPT 06 — Near-field trackside prop kit (the sense-of-speed kit)

Write Blender Python that procedurally models, UVs, and exports to GLB (3 LODs each, shared 4096 trim-sheet PBR atlas you also generate) a modular trackside kit at real scale: street-light pole with sodium lamp head (emissive mesh separated) 9 m; shorter harbor bollard light 1 m; armco guardrail 4 m segment with posts and a curved variant; concrete jersey barrier 3 m with red/white and plain variants and tire-scuff decals; chain-link catch fence panel 3 × 3.5 m with alpha-tested mesh; tire-wall stack segment; sponsor banner frame 6 × 1 m with a swappable banner texture slot; distance boards 150/100/50; corner marshal post; traffic cone; plastic water barrier; parked enclosed trailer; portable floodlight tower (emissive, for night); grandstand section low-poly; shipping containers ×3 colors; palm trunk base planter. Everything instancing-friendly, pivot at ground centre, single material where possible. Include a placement script that lays poles/fence/barriers along a supplied route spline at set spacing and offsets so the road edge has constant rhythm within 2–6 m of the racing line.

---
Shared rules for every asset: real-world scale; seamless tiling where stated; PBR metal/rough workflow (baseColor sRGB, normal OpenGL +Y, packed ORM = occlusion R / roughness G / metallic B); no baked lighting or shadows in baseColor; no text, logos or watermarks unless the prompt asks; deliver PNG sources plus KTX2 (Basis UASTC for normals, ETC1S for color) and a one-line README with texel density. Put outputs under public/assets/p11/<asset-name>/ and register them in the asset manifest. Do not overwrite historical assets.
# ASSET PROMPT 07 — Wooden SlingMods sign (fix the oversized logo)

The SlingMods logos currently pasted on the wooden signs are too big for the boards. Rebuild the sign asset: a weathered cedar plank sign on two posts (board 2.4 m × 0.9 m, 5 horizontal planks with gaps, rusted bolt heads, end-grain, sun-bleached top edge), 2048 PBR set. Apply the existing logo file public/assets/brand/slingmods-logo-main.png (do NOT redraw or alter the logo) as a painted-on graphic: preserve aspect ratio, centered, scaled to fit inside the board with 12% padding on every side, never crossing the board edge or the posts. Make it read as paint on wood: multiply over the grain, paint breaks up over plank gaps and cracks, slight flaking at edges, roughness a touch lower than bare wood. Provide a second variant with a routed/carved-in logo filled with paint. Export GLB with 2 LODs and replace the existing sign instances in the scenery source, then re-export the route GLBs. Add a test that measures the logo quad's bounds against the board's bounds so this cannot regress.

---
Shared rules for every asset: real-world scale; seamless tiling where stated; PBR metal/rough workflow (baseColor sRGB, normal OpenGL +Y, packed ORM = occlusion R / roughness G / metallic B); no baked lighting or shadows in baseColor; no text, logos or watermarks unless the prompt asks; deliver PNG sources plus KTX2 (Basis UASTC for normals, ETC1S for color) and a one-line README with texel density. Put outputs under public/assets/p11/<asset-name>/ and register them in the asset manifest. Do not overwrite historical assets.
# ASSET PROMPT 08 — UI / HUD art kit

Generate a cohesive UI kit as SVG (plus PNG @2x where raster is needed), inspired by the Polaris Slingshot instrument cluster and the SlingMods brand (black, white, signal red #e0202a accent, graphite greys): (1) chamfered-corner panel frames in 3 sizes, 9-slice ready, with a subtle brushed-graphite or carbon texture and a 1 px red rule accent; (2) a tachometer ARC (0–8k, redline zone, needle and sweep-fill variants), big gear numeral style, speed readout, shift-light row; (3) minimap frame and player/rival/points markers; (4) position tower rows with rival color tabs; (5) boost meter with three drift tiers (white / orange / red) and a full-charge flare; (6) SlingMods Points token icon + counter + combo multiplier badge; (7) sector split flags (green/red/purple); (8) icon set, 24 px grid, 2 px stroke: build, paint, lighting, suspension, exhaust, aero, storage, destinations, shop, photo mode, replay, settings, audio, controller, keyboard, day, dusk, night, rain; (9) letterbox bars and a lower-third name plate for rival intros; (10) loading screen layout: car silhouette on turntable, progress rule, tip line. Condensed uppercase grotesque type (suggest an open-licence font and include it). No generic rounded-rectangle game UI.

---
Shared rules for every asset: real-world scale; seamless tiling where stated; PBR metal/rough workflow (baseColor sRGB, normal OpenGL +Y, packed ORM = occlusion R / roughness G / metallic B); no baked lighting or shadows in baseColor; no text, logos or watermarks unless the prompt asks; deliver PNG sources plus KTX2 (Basis UASTC for normals, ETC1S for color) and a one-line README with texel density. Put outputs under public/assets/p11/<asset-name>/ and register them in the asset manifest. Do not overwrite historical assets.
# ASSET PROMPT 09 — VFX flipbooks and particles

Generate flipbook sprite sheets (8×8, 2048², RGBA, with matching normal where useful, authored on black/transparent, looped where stated): tire smoke puff (white-grey, 64 frames); thick burnout smoke (looping); dust/dirt kick-up (tan); dry-leaf burst (for the ridge route); water spray/rooster tail for wet roads; brake/scrape sparks burst (warm white → orange); drift sparks in three tints (white, orange, SlingMods red); exhaust flame pop (blue core to orange, 16 frames); heat-shimmer distortion normal (looping, tileable); boost speed-lines radial (looping); points-pickup burst (red/white shards + ring); confetti for podium; lens dirt + anamorphic streak textures for bloom; raindrops-on-lens for the wet presets. Also a soft round particle, a streak particle, and a 3D noise tile for volumetric light shafts and ground fog.

---
Shared rules for every asset: real-world scale; seamless tiling where stated; PBR metal/rough workflow (baseColor sRGB, normal OpenGL +Y, packed ORM = occlusion R / roughness G / metallic B); no baked lighting or shadows in baseColor; no text, logos or watermarks unless the prompt asks; deliver PNG sources plus KTX2 (Basis UASTC for normals, ETC1S for color) and a one-line README with texel density. Put outputs under public/assets/p11/<asset-name>/ and register them in the asset manifest. Do not overwrite historical assets.
# ASSET PROMPT 10 — Skies / HDRIs per time of day

Generate or source (CC0 only, e.g. Poly Haven; record provenance) equirectangular HDR skies at 8k for background and 1k prefiltered for lighting, for a Southern-California coast: Golden Hour (sun 8° above the ocean horizon, warm, long shadows, scattered high cloud); Dusk / blue hour (sun just set, orange band at horizon, deep blue overhead, first stars); Clear Night (moon, stars, faint marine haze, city glow on one horizon); After Rain overcast-breaking (dramatic cloud, shafts of light, wet-look justification); Marine-layer morning (soft grey, low contrast, for fog preset). For each, supply the matching sun/moon direction, color temperature, intensity and fog color as JSON so lighting rigs match the sky exactly.

---
Shared rules for every asset: real-world scale; seamless tiling where stated; PBR metal/rough workflow (baseColor sRGB, normal OpenGL +Y, packed ORM = occlusion R / roughness G / metallic B); no baked lighting or shadows in baseColor; no text, logos or watermarks unless the prompt asks; deliver PNG sources plus KTX2 (Basis UASTC for normals, ETC1S for color) and a one-line README with texel density. Put outputs under public/assets/p11/<asset-name>/ and register them in the asset manifest. Do not overwrite historical assets.
# ASSET PROMPT 11 — SlingMods Points token (3D pickup)

Model a collectible token as GLB (≤ 800 tris, 1 LOD): a thick coin/gear hybrid 0.6 m diameter, brushed dark metal rim with chamfer, red enamel face, the SlingMods 'S' mark or three-wheel glyph debossed (use the existing brand asset, do not invent a new logo; if unavailable use a neutral three-spoke wheel glyph). Emissive red inner ring so it blooms at night. Three value variants distinguished by rim color: steel (10), gold (25), red-chrome (50), and a larger animated 100 with an orbiting ring. Idle animation: slow spin + bob; pickup animation: quick scale-up, flash, shatter into shards. Must read clearly at 100 mph from 80 m away against both asphalt and foliage, day and night.

---
Shared rules for every asset: real-world scale; seamless tiling where stated; PBR metal/rough workflow (baseColor sRGB, normal OpenGL +Y, packed ORM = occlusion R / roughness G / metallic B); no baked lighting or shadows in baseColor; no text, logos or watermarks unless the prompt asks; deliver PNG sources plus KTX2 (Basis UASTC for normals, ETC1S for color) and a one-line README with texel density. Put outputs under public/assets/p11/<asset-name>/ and register them in the asset manifest. Do not overwrite historical assets.
