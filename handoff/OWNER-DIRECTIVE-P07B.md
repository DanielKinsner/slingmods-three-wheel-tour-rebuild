Continue the existing SlingMods: Three-Wheel Tour rebuild using the
attached SlingMods-Astra-Director-Review-14.zip.

Read CODEX_NEXT.md, the P07B packet, and the current checkout’s AGENTS.md,
HANDOFF.md, production state, and latest review evidence. Preserve newer
work. Do not restart the project, repeat completed milestones, or reset
to an older review commit.

ADDITIONAL OWNER DIRECTIVE — COMPLETE CROSS-MACHINE GIT HANDOFF

I will continue this project on a DIFFERENT MACHINE with a FRESH AGENT
SESSION. Before you finish this turn, everything needed to build, run,
edit, test, and continue directing the current game must be recoverable
from the Git remote—not dependent on this computer or this conversation.

I explicitly authorize committing and pushing the intended project work
and handoff materials to the existing private rebuild repository. This
overrides the Review14 packet’s default no-Git-push restriction ONLY for
this project handoff.

Expected repository:
DanielKinsner/slingmods-three-wheel-tour-rebuild

Discover and verify the actual checkout, remote, branch, and current
state. Do not confuse it with the original game repository. Use the
established handoff branch; prefer main if that remains the project’s
intended integration branch. Preserve remote work and local user edits.
No force pushes, destructive resets, unrelated changes, or history
rewrites.

1. COMPLETE THE AUTHORIZED REVIEW14 WORK

Execute P07B within its existing boundaries, then close out the handoff.
Git-transfer authorization is NOT authorization to publish a website,
change an account or plan, spend money, or modify an existing deployment.

If hosting approval or access is still missing, complete the local
launch/preflight and Git handoff anyway. Record hosting as pending rather
than blocking the entire transfer or claiming a public URL exists.

2. AUDIT WHAT A FRESH MACHINE ACTUALLY NEEDS

Inspect tracked, untracked, and ignored files before selecting changes.
Do not assume .gitignore currently distinguishes essential assets from
disposable output correctly.

Ensure the remote contains:
- Current game source, configuration, lockfile, tests, and build scripts.
- Every required runtime model, texture, logo, audio file, data file,
  manifest, and other asset—not only the small lean-review selection.
- Current editable Blender sources, their required dependencies, and
  authoring/export scripts. Resolve packed/external asset dependencies.
- Required license/attribution records and asset provenance.
- Current director instructions, Review14 acceptance/limitations,
  production state, and the evidence needed to understand the next step.
- Reproducible demo-build, preview, and publication-staging instructions.

A pointer to an ignored folder on this machine is NOT a transferred asset.
A filename or hash alone does not make its bytes remotely recoverable.

Do not blindly commit the whole working directory. Exclude credentials,
tokens, browser profiles, personal saves, node_modules, downloaded tools,
caches, redundant ZIPs, and disposable build output. Preserve historical
material locally; do not delete it merely to reduce the handoff.

Reproducible generated output may remain excluded only when the fresh
clone can regenerate it from tracked inputs. Clearly distinguish the
original reviewed output from any newly rebuilt candidate.

If an essential file cannot be pushed, do not silently omit it. Use an
already-authorized repository storage mechanism and verify retrieval,
or report the exact blocker. Do not introduce paid storage.

3. MAKE THE NEXT SESSION SELF-SUFFICIENT

Update the existing HANDOFF.md and current authority/state documents
rather than creating competing instructions.

Record:
- Correct repository and branch, runtime identity, and review baseline.
- What this turn changed, what is complete, and what remains pending.
- Exact setup, build, test, demo-build, and local-launch commands.
- Required tool versions and how authoring tools are located.
- Repository-relative locations of current editable and runtime assets.
- Known defects, evidence limitations, and unresolved hosting approval.
- The single next assignment for the incoming agent.
- What intentionally does not transfer, including personal browser saves.

Include a short NEW-MACHINE RESUME PROMPT. It must tell the next agent
to discover its checkout/tools, read the current handoff, and continue
without restarting or repeating completed packets.

Remove active dependence on this computer’s username or absolute paths.
Keep historical capture paths unchanged as provenance.

4. PROVE THE TRANSFER FROM THE REMOTE

Commit and push, then create a genuinely fresh clone FROM THE REMOTE
in an isolated temporary directory. A worktree or copy of this local
repository is not sufficient.

From that clone:
- Verify the intended branch and commit.
- Retrieve any required repository-managed large-file objects.
- Install the pinned dependencies.
- Run the build and full test suite.
- Generate and launch the current demo using the documented commands.
- Perform isolated browser smoke tests for entry, race, retry, garage,
  required assets, and the product link.
- Verify required editable sources and dependencies are present.

Do not copy missing assets, node_modules, ignored output, or undocumented
configuration from the original checkout to make the test pass.

Repair any missing tracked inputs or portability defects, push the
repairs, and repeat the affected verification. Keep original evidence
unchanged. Disclose any shared package cache or host-installed tools.

Record the exact tested commit. Commit/push the validation receipt and
handoff updates, distinguishing documentation-only follow-up commits.
Verify the final remote branch tip and report any remaining local work.

A fresh clone on this computer proves repository recovery here; do not
claim it has already been tested on the destination machine.

FINAL RETURN

Give me one concise handoff containing:
- Verified repository, branch, final pushed SHA, and tested runtime SHA.
- Fresh-clone verification results and any precise remaining blockers.
- Actual local demo address, and hosted URL only if authorized and tested.
- The location of HANDOFF.md and the new-machine resume prompt.
- The exact next action for the incoming agent.

Do not return another giant backup ZIP. Keep any review upload within
the packet’s lean-delivery limits. The full working project belongs in
the verified Git handoff; the upload is only the review evidence.

Continue autonomously through implementation, validation, repair, and
push. Do not ask me to run routine tests or take over my desktop.