# Phase 2E - the car touches the world

- All road modes share pooled tire smoke, off-road dust/leaves, wet spray, actual barrier-contact sparks, bounded session skid marks, night brake heat, headlight fog, and fitted-exhaust lift pops/local heat shimmer. All inputs are read-only telemetry; Sport v5, steering, rivals, rewards and save schemas are unchanged.
- Six optional P11 KTX2 atlases (1.56 MB total) are allowlisted and warmed on all routes. Missing textures use a procedural fallback. Low reduces emission density and disables fog/shimmer; reduced motion disables exhaust flashes/shimmer. `&effects=off` is the comparison switch. Pools are shared across cars; rivals emit at 28% density. At most nine effect draws per scene pass, with no additional render pass. Skids retain the newest 3,072 segments until leaving the road scene.
- Typecheck and 360 tests pass. Real fixed-step driving covers Harbor test, barrier scrape, Express race, Ridge test, Chapter 01, published look/quality/effects options, and Low/reduced-motion with all six textures blocked. Separate synthetic presenter fixtures verify smoke/skids, dust/leaves, hot rotors and correctly mounted dual exhaust pops without altering physics/race state. Scripts: `verify-phase2e.mjs` and `capture-phase2e-fixtures.mjs`; both accept `BASE_URL` and `EVIDENCE_DIR`.
- Fixed the published URL whitelist discarding existing time-of-day and quality presets; valid options now reach the shared renderer. Both the packaged driving checks and the separate visual fixtures pass.
- Sequential 1440p Harbor dusk High four-car samples: effects on 10.98 ms average / 16.6 ms p99; off 11.03 ms / 21.5 ms p99. No measurable average regression in this sample. These are short local browser samples, not hardware-wide or sustained-performance certification. The earlier phase-wide performance HOLD is not silently cleared.
- See `PHASE-2E-VALIDATION.json` for package and verification identities. Next slice is 2F (contact shadows / AO and chase-camera shadow tuning), then 2G/2H. Phase 2E introduces presentation estimates, not physically measured brake temperature, fluid simulation, or a claim about real exhaust fitment/flames.
