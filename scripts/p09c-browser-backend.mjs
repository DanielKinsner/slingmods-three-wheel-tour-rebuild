import {chromium} from '@playwright/test';import fs from 'node:fs/promises';
const base=process.env.BASE_URL||'http://127.0.0.1:5202',out=process.env.EVIDENCE_FILE;if(!out)throw Error('EVIDENCE_FILE required');
const browser=await chromium.launch({headless:true,args:['--use-angle=d3d11','--mute-audio']}),page=await browser.newPage({viewport:{width:1280,height:720}});
try{await page.goto(base+'/?scene=signature&test=1&profile=1');await page.waitForFunction(()=>window.__SIGNATURE?.ready,null,{timeout:120000});const backend=await page.evaluate(()=>{const canvas=document.querySelector('canvas'),gl=canvas.getContext('webgl2'),ext=gl.getExtension('WEBGL_debug_renderer_info');return{renderer:gl.getParameter(gl.RENDERER),vendor:gl.getParameter(gl.VENDOR),unmaskedRenderer:ext?gl.getParameter(ext.UNMASKED_RENDERER_WEBGL):null,unmaskedVendor:ext?gl.getParameter(ext.UNMASKED_VENDOR_WEBGL):null,version:gl.getParameter(gl.VERSION),userAgent:navigator.userAgent,hardwareConcurrency:navigator.hardwareConcurrency}});await fs.writeFile(out,JSON.stringify({browser:browser.version(),backend,method:'Separate read-only backend probe with the same Chromium/D3D11 launch configuration, after native matrix; no concurrent scored race.'},null,2));}finally{await browser.close()}
