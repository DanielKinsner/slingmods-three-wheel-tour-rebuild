import{readFileSync,readdirSync,writeFileSync}from'node:fs';import{createHash}from'node:crypto';import{execFileSync,spawnSync}from'node:child_process';import assert from'node:assert/strict';
function files(d){return readdirSync(d,{withFileTypes:true}).flatMap(x=>x.isDirectory()?files(d+'/'+x.name):[d+'/'+x.name])}
const paths=[...files('src'),...files('tests'),...files('public/assets/drivers'),...files('public/assets/audio/p03b2'),'public/assets/vehicles/slingshot-p03a2.glb','public/assets/inspection-bay-p03a1.glb','public/assets/test-pad.glb','public/assets/practice-p03b1.glb','public/assets/slingshot-contact-layout.json','package.json','package-lock.json','index.html','vite.config.ts','tsconfig.json','scripts/build-review05.mjs','scripts/capture-review05.mjs','scripts/verify-first-drive-audio.mjs'];
const sha=b=>createHash('sha256').update(b).digest('hex'),snapshot=()=>Object.fromEntries(paths.sort().map(p=>[p,sha(readFileSync(p))])),before=snapshot(),commit=execFileSync('git',['rev-parse','HEAD'],{encoding:'utf8'}).trim();const result=spawnSync('npm run build',{shell:true,cwd:process.cwd(),stdio:'inherit'});assert.equal(result.status,0);assert.deepEqual(snapshot(),before);writeFileSync('dist/review-build.json',JSON.stringify({commit,created:new Date().toISOString(),inputs:before,method:'Selected P03B2 runtime, tests and evidence inputs; unchanged before/after TypeScript/Vite build. Historical unused public variants are excluded from this review manifest.'},null,2));console.log('Frozen Review05 build '+commit);
