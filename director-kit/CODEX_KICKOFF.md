# Start a new game — not a continuation

Act as the local production director for **SlingMods: Three-Wheel Tour**, implementing Astra's fresh-start director kit for Dan.

**Start from scratch in a NEW directory and a NEW local Git repository. Do not extend, refactor, migrate, or import yesterday's game.** Read `FRESH_START_POLICY.md` first, then `START_HERE.md`, this kit's `AGENTS.md`, `docs/01_GAME_DESIGN.md`, `docs/04_ORCHESTRATION.md`, `production/state.json`, and P00 in `production/WORK_PACKETS.md`.

Use `slingmods-three-wheel-tour-rebuild` as the default new project name. If the kit was opened in an old game's workspace, leave it untouched and create a separate project outside that Git root within available permissions. Copy only this kit. Never clear an existing directory or inherit old `.git`, source, assets, saves, or deployment settings. Respect applicable workspace/platform instructions without treating historical game requirements as current scope.

Dan delegates routine creative and engineering decisions. Do not ask him to choose models, colors, engines, physics settings, features, or task order. Execute the decisions in the kit, inspect actual results, and revise weak work. Use background Blender commands and isolated browser automation; do not take over his OS mouse/keyboard, focus his browser, or make him your QA operator.

This is an EXECUTION request, not another proposal. Start P00 with a fresh TypeScript/Vite/Three.js/Rapier foundation, lightweight DOM/CSS UI, new save namespace, and a real Blender export-to-runtime calibration. Verify current installed APIs before using them and lock the tested dependencies. No inherited Vue requirement and no old save migration. Leave the storefront and all old deployments untouched.

Use at most three non-conflicting workers. Complete G0, then work on G1 recognition and G2 test-pad dynamics. Make a recognizable 2024 Slingshot R AutoDrive that genuinely drives, with actual Blender sources/exports and correct three-contact geometry. The Spyder F3-T and Ryker Rally receive reference/scale blockouts first, not rushed final reskins. Never advance past missing evidence or a failed criterion.

The first complete slice is one finished Slingshot, one premium garage, a newly authored short Biscayne Harbor circuit, three rivals, day/night, one newly written story beat, and three compatible real products. No giant city, second finished vehicle, expanded campaign, fake nitro, or feature flood before it passes. A primitive car disguised by bloom fails visual review. A vehicle pinned to a spline fails physics review.

Record test outcomes and runtime captures honestly. A separate reviewer must inspect the artifacts; the included validator checks evidence integrity, not aesthetic quality. After repeated failure, change the specific approach while scope is small. Keep known-good checkpoints of this NEW build. No credential exposure, new spending, real-store writes, or production deployment. Existing paid tools require an already authorized bounded use.

Begin implementing P00 now, then continue the earliest runnable packets. At a session boundary, record what actually works, what fails, the evidence, and the exact next executable task in state.json. Do not promise unattended future work.
