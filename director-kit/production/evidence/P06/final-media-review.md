# Independent final media audit

Audited existing `video-final` evidence for runtime `8d3931f40bf472666c7f2595cfa2650bb2b81980`, showcase kit `cd4f163c960c9acf0b963748edad74ba83787666fe2f79c01d719c0b3793c869`. This audit read capture/mux code, recorded telemetry, metadata, and four existing runtime PNGs; independently ran ffprobe and SHA-256 checks. No runtime edits, browser runs, encoding, full-file signal decode, desktop capture, or human listening were performed.

## Technical findings

- Final MP4 is 48,834,793 bytes, H.264 1280×720 at 25 fps / 4,806 frames / 192.240 seconds plus stereo AAC 48 kHz / 192.234 seconds. Actual file hashes match all three recorded source/final hashes. Final SHA-256: `4c1249ac145c1337ac7e34b7d03eb8ace2a12770c0aff7800f6459049cbe5f91`.
- The native MediaRecorder destination receives the same mixed player and nearest-two spatial rival buses used by the game. It adds one capture branch; it does not route captured sound back into the audible graph. No microphone, system loopback, offline engine rendering, dubbed engine track, time stretch, or replaced race outcome is used by this capture/mux script. Three disclosed diagnostic oscillator chirps ARE added to the recording only, paired with magenta DOM flashes; they are alignment signals, not game sound.
- Reviewed alignment uses three 20 ms audio correlation windows with 5 ms hops near expected chirps and 25 fps flash detection. Median constant shift is +3.195 seconds. Recorded anchor residuals are approximately -5.667 ms, +0.021 ms, and 0 ms. The visual observations are quantized to 40 ms; these residuals do not demonstrate sub-frame perceptual accuracy. Mux copies the video stream, delays and pads actual captured audio, then encodes AAC. Original decoded live-audio duration is 181.14 seconds inside the 192.24-second movie; bay/loading outside that recording is silent.
- Prior source-audio signal analysis reports stereo peak 0.249/0.236, RMS 0.0391/0.0389, zero clipped/nonfinite samples. This audit checked that the analyzed source bytes match but did not independently repeat the full decode. Neither statistics nor graph tests constitute human listening approval.
- Provenance lists 53 served asset hashes matching its manifest. The record used isolated Chromium 153.0.8010.12 at 1280×720, seed 11 and copied historical earned save fixture. Native RAF and ordinary input resolver/camera consume virtual device samples. Camera events show near → far at 16.03 s → cockpit at 23.02 s → held rearward at 28.02 s → cockpit at 28.82 s → near at 39.53 s. No controlled clock or reference-camera shortcut is invoked in the reviewed capture script. This is automated virtual-input coverage, not physical-controller verification.
- Recorded field finishes two valid laps for Maya, Jett, player and Nico; player places third at 2:45.903. No recorded page errors. The result legitimately awards 150 placement + 400 first-podium credits, adding 550 to the historical fixture's 300. Reload preserves balance 850, owned/equipped cyan SM-133 at 60%, and chapter clear.

## Presentation findings and limits

Inspected `grid.png`, `cockpit.png`, `result.png`, and `saved-build.png` directly. Compact HUD leaves the road visible; cockpit has fitted hands and forward sightline; cyan underglow reads on the road and garage floor. Result and saved-product controls remain readable at 720p. Rear highlights on the red rival are harsh; night garage remains dark and distant course areas visually spare. These stills do not substitute for a full motion review or grant visual gates.

**Unresolved label defect in the audited capture:** grid, cockpit and result visibly say `SILENT` in the bottom evidence caption. The capture script inserts that text even when it records the live graph, and the final continuous film is sourced from this capture. That label misrepresents the audio-bearing final film. Preserve the original evidence, explicitly correct or disclose the label before treating it as polished presentation evidence. No correction was made during this read-only audit.

`video.json` is a pre-mux description that still says synchronization pending; final MP4 and `audio-video-verification.json` establish completion. The generic `provenance.method` also says no recording/readback in the scored loop, while `run.record` is true. This recorded evidence lane includes recording and screenshot overhead and must not support clean production performance claims. Separate clean wall-clock runs remain authoritative for performance.

Companion `final-media-review.json` preserves compact exact telemetry, track probe, camera events, hashes and audit boundaries. Technical track/provenance audit is supported; final labeling, human listening, full motion visual approval, production performance, G3 and G4 approval are not conferred.

## Caption-corrected delivery addendum

Independently rechecked `video-final/complete-race-LIVE-AUDIO-captioned.mp4`: **50,921,186 bytes**, SHA-256 `ca9160cdf112bcad450ac2f4ab0e0630b9cb6c415c85c4144dfe74156d7e21f9`. Source and derivative hashes match `caption-correction.json`; source remains the previously audited original. Lightweight ffprobe confirms the same 4,806 frames at 25 fps, 1280×720, video duration 192.240 s and stereo AAC 48 kHz duration 192.234 s.

The reviewed correction script places one opaque 380×30 footer rectangle at (450,684), then Arial text `GAME AUDIO / VIRTUAL INPUT / SILENT BAY`. It performs no frame selection, timing edits, outcome changes or camera changes. It re-encodes video, so pixels outside the footer can differ through compression; “same frames” refers to sequence/timing rather than bitwise image identity. Audio uses stream copy; the producer additionally asserts identical decoded PCM SHA-256 `cfeb3b828212a0fc2c408542fba03560c5d66b113a175a5e0d028abd3637abc2`. This lightweight audit verified matching artifact hashes and inspected that assertion but did not repeat decoding during clean performance measurement.

The delivered derivative addresses the original misleading label through a disclosed annotation correction. No new derivative frame was extracted or visually inspected in this addendum. Original stills/film remain retained and still show the old footer; those originals are historical source evidence, not a claim of absent audio. `video-original.json` and `provenance-original.json` preserve prior wording; corrected metadata now identifies this as recorded overhead-bearing evidence, not scored performance. The provenance manifest is unchanged. `post-freeze-review-tooling.json` separately discloses capture-helper caption and bay-probe wording changes; runtime remains `8d3931f`.

Use the captioned MP4 as primary delivery, with this correction record. No human listening, full motion visual, production-performance or G3/G4 approval is added.
