# Astra Review11 — P06 Harbor Showcase

Project: `C:\Users\SM - Dan\Documents\GitHub\slingmods-three-wheel-tour-rebuild`
Captured runtime commit: `8d3931f40bf472666c7f2595cfa2650bb2b81980`. Packaging commit is recorded in PACKAGE-MANIFEST.json. This is the existing rebuild, not the old game. G3/G4 remain pending Astra; no release, spending or deployment.

## Run

```powershell
Set-Location -LiteralPath 'C:\Users\SM - Dan\Documents\GitHub\slingmods-three-wheel-tour-rebuild'
npm ci
npm run build
npm run preview
```

Open `http://127.0.0.1:5187/`. Review used Node24.15.0/npm11.6.2, locked dependencies, Windows11Pro26200, i9-12900K, RTX4080 driver32.0.16.1692 and isolated Chromium153.0.8010.12/ANGLE D3D11. No personal browser or desktop/mouse control. The local preview was restored after the Codex app interruption without rebuilding or restarting the project.

## Playable result

The existing first chapter remains complete: fresh solo lap earns800 gamecredits; later unique valid laps100; optional TricLED SM-133 base underglow costs600. Preview, purchase/install, saved color/power/brightness/ownership and deliberate real-product link remain. The night crew race has three actual independently controlled rivals sharing the player's Rapier world, two validated laps,300/200/150/100 placement awards and a one-time400 first-podium bonus. Immediate retry and saved garage return work. Near/far/cockpit and held look-back remain. No additional vehicles, products, opponents or chapters.

P06 adds an original Blender terminal, marina, service section, promenade and far shore on the unchanged course, plus the compact showroom, actual UV/PBR maps and animated water normals. Spatial instance cells/frustum culling and shared materials keep authored repetition inexpensive. The accepted hero, rear repair, driver, road/colliders, physics, race rules, audio RPM mapper and saves are preserved. Headlight calibration reduces rear wash. The ordinary menu/HUD is compact, rivals have stable color-coded order rows and proximity cues, help is controller-accessible, and build controls fit/scroll at720p/1080p. Done closes preview safely and returns focus to Build.

## Start with these artifacts

- `director-kit/production/evidence/P06/video-final/complete-race-LIVE-AUDIO-captioned.mp4`: continuous native wall-clock grid→two valid laps→third place2:45.903→850-credit saved cyan garage. Ordinary virtual input and production rivals; not human play or scored performance. Existing earned-v1 fixture is disclosed. The fresh zero-credit earn/purchase/podium/reload path is separately proved in fresh-verified.
- `director-kit/production/evidence/P06/day-verified/garage-day-SILENT-fixed.mp4`: brief actual garage/build stock-versus-installed and ordinary day driving; explicitly SILENT.
- `district-comparison.jpg`, `headlight-contact-sheet.jpg`, bay-final and lighting-final: current runtime comparisons, not Blender renders. Spacing is3/8/20m bumper clearance; center separation adds measured vehicle length. Cameras, exposure, gray swatch, fixtures and hashes are recorded. Calibration is stationary presentation evidence, not driving/performance.
- `tests-final.log`, preservation-final.json, parity.json, lifecycle-verified, final-transitions, fresh-verified, crew-ui-final-clock-proof, controller-verified, audio-verified and solo-ui-verified contain current bounded checks.

## Audio and capture honesty

The film contains the actual live player and two nearest spatial-rival buses through a native MediaRecorder tap, not offline replacement engine sound, microphone or system loopback. Stereo48kHz has zero clipped/nonfinite decoded samples. Three disclosed evidence-only chirps/DOM flashes align within one25fps video frame; the small numerical residual is not sub-frame perceptual proof. Human sound-quality/hardware listening remains unapproved. The bay/loading has no game audio graph and is silent.

An inherited SILENT evidence footer was incorrect. The delivery film replaces only that footer; it retains the continuous sequence/timing and stream-copied audio, with decoded PCM hash identity checked. The source film/stills are retained locally; original stills retain that old footer. caption-correction.json and final-media-review describe the exact processing. The first full recording missed its initial visual sync flash and was retained; the successful recapture delays that marker. The secondary movie's first encode failed fontconfig; the same original frames were re-encoded using the installed Arial font, with no content/time substitution.

## Validation and limits

111 automated tests pass. The4200-row solo replay is byte-exact. Four completed races/three retries maintained197geometry/52texture objects; three document transitions and additional final-bay transitions pass. The preserved original UI test lacked a finish; an independently reproduced controlled-clock/native-key timestamp mismatch explains a concrete harness failure shape. The clock-aligned derivative passes unchanged assertions plus fresh-reset/neutral checks under the same9s delay. It does not invent the missing first failure timestamps. No runtime physics/input change was required.

All five completed configurations / seven races meet the working targets: p95 16.7ms and p99 16.8ms, zero racing intervals over100ms and zero clipped simulation time. The heavy repeat retains one50ms interval. Programs rise20 to21 once early, then stay stable; exact variant cause remains a preparation reservation. Full native results and every startup/racing outlier are in performance-summary.json. The app interruption left one incomplete heavy repeat without buffered samples, then a connection-refused retry before the local preview was restored. Both are explicitly unscored and preserved. No recorder/readback/controlled clock/full inspect occurs inside scored racing. User applications were left untouched; no globally idle-host or GPU-time claim. Historical Review10's366.6ms outlier and33.4/50ms heavy repeat remain unresolved observations, not erased by later clean runs.

The harbor remains economical/stylized: broad flat inland areas, simple distant silhouettes and polygonal palms. The showroom service-side opening is dark. Common rear hardware can still look bright close to headlights; paint identity is strongest in accents/side views and HUD. Hero fidelity, synthetic sound quality, mirrors/instruments, underglow photometry and physical-controller feel retain prior review limits. These are not automatic G3/G4 approvals.

## Reproducibility and Git

Current new Blender sources are included under assets/blender/showcase; unchanged large originals are listed by exact path/hash in unchanged-blender-source-locations.json. Full historical ledger/evidence remains local/in existing history; this ZIP contains a marked current ledger excerpt and curated evidence. PACKAGE-MANIFEST binds every packaged file's size/hash.

Runtime source/config/public assets remain identical to the captured build. Two post-freeze review-helper changes correct metadata/annotation only, documented with old/new hashes and diff in post-freeze-review-tooling.json; current review tooling and extra read-only validators are included. They do not alter the served game or scored input/collector logic.

All intended work is on local main. Generated movies/bulk traces were removed from the index only and remain locally/in history; dependencies/dist/tools/caches/backups/archives/secrets are ignored. Three pre-existing unrelated untracked files remain untouched. Push destination is still unconfirmed because the rebuild has no remote; do not substitute the older game's repository. See AUTONOMOUS_RESUME.md for the exact remaining Git action if no destination response arrives.
