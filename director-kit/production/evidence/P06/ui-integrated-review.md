# P06 integrated UI review

Actual production scene and UI inspected in fresh isolated headless Chromium at 1280x720 and 1920x1080. No desktop/profile takeover, performance benchmark or controlled clock. Fresh root had zero credits. Crew UI used a copied prior earned save fixture; this is eligibility/migration coverage, not a newly earned reward. Native buttons/keyboard and standard virtual controller samples exercised the current menu/input path. Latest reviewed images are in `integrated-ui-03/`; their source, built bundle and image hashes are recorded in `review.json`.

Both sizes completed 11 product-control visibility, scroll and DOM hit-target assertions, including the deliberate real-product link. Closing Build restores focus to the visible chapter Build action. The Done button now closes and cancels temporary preview directly, while Esc/back retains its staged preview-cancel behavior. The saved appearance is unchanged by unowned preview; this is also asserted in the focused browser test. Duplicate entry toast is suppressed because the chapter panel already introduces the same objective; its career entry transaction remains unchanged, and first-build product dialogue remains available. The native Back to chapter button now matches the secondary button style. Lead hid legacy Inspect/Drive/Bay navigation in ordinary garage mode.

Controller D-pad reached the crew help summary and bottom-face opened it, with no unintended race start. Native start/skip/countdown, brief W driving and Esc pause exercised all four participants in the ordinary running scene. No page errors were reported. The focused suite passed 12/12 again (`interface-integrated-tests.log`), including held/replacement controller rearming, pending transaction navigation, failed writes, native help/mute focus and stable standings DOM. Build/typecheck passed (`ui-final-build.log`).

Visually inspected cleaned root720, build-link720, time-trial-entry720 and HUD1080 images, plus the preceding 720/1080 root/build/help/grid set. UI is readable, the car has clear room, the product controls scroll fully above the viewport bottom, root offers only the current three actions, and normal screenshots contain no candidate/gate/hash labels. The developer Inspection tools button remains available as requested. Help is collapsed by default. Race HUD stays at the edges; the small Nico-left cue corresponds to the nearby side car.

This UI review does not grant whole-scene or lighting approval. Jett's close rear surfaces still look very bright under player headlights in the captured running view; lead's matched lighting review is responsible for the material/light acceptance. Garage sign is partially cropped at the top of the normal camera; the car and menus remain legible. No human controller, enjoyment, audio-quality, final-race result or full-lap performance approval is claimed here.

## Harness history

The first integrated capture exposed the two-stage Done/preview behavior and was retained in `integrated-ui/`. Corrected `integrated-ui-02/` completed normally with 16 captures and a report. After entry/navigation cleanup, `integrated-ui-03/` completed both viewport loops, all runtime assertions and all 16 images, then its metadata collector encountered EISDIR while trying to hash a directory under dist/assets. The error log is retained. The collector now filters regular bundle files; metadata was separately completed with explicit disclosure rather than pretending the earlier collector succeeded. No runtime assertion was removed or weakened. The capture script and evidence are pre-final-freeze working build material, so provenance uses hashes rather than the old working commit label.

No further UI edits are pending from this review. Lead may freeze the integrated source and proceed with final validation.
