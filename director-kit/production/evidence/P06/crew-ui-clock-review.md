# P06 crew UI regression: controlled clock diagnosis

The first `ui-verified.log` failed because no valid player result was found after the driver's deadline. It lacked checkpoint state, so its exact event timestamps and focus state remain unavailable. A later passing diagnostic alone did not establish cause.

A preserved derivative then waited nine real seconds immediately before the native held-R check while leaving the original manual test clock unchanged. Native KeyR timestamp was 12,670.5 ms, but all 70 hold frames ran at manual times 6,700–7,883.3 ms. The production input resolver correctly refused to count a hold from a future timestamp. Attempt ID stayed unchanged and the race never restarted. The following virtual controller arrived already holding throttle in a running race, never received its required neutral baseline, and never took ownership. Keyboard remained active with zero throttle through 281.65 seconds of race time. Document focus stayed true. The unchanged valid-result assertion failed again. This establishes a concrete harness bug and reproduces the failure shape; it does not invent missing observations for the first failure.

`scripts/verify-crew-ui-p06.mjs` preserves the original script and assertions. It aligns the manual clock to native performance.now before and immediately after the native R event, verifies a new attempt and countdown after the hold, and explicitly samples/asserts a focused, neutral, connected controller before driving. No game/runtime input, reset timing, physics, race rules, controller tuning or assertions were weakened. Any catchup interval from aligning the controlled clock remains a functional-test artifact, never performance evidence.

Both the aligned comparison and the final stronger proof passed under the same disclosed nine-second wall delay. Final proof produced a real valid two-lap third place at 165,902.9699 ms, 850 saved credits from the existing 300-credit fixture, immediate fresh-attempt retry without duplicate credit, and saved cyan/chapter reload. All original blur/zero-world-step intro, pause/held-W rearm, native cameras/lookback, reward and reload assertions passed. Final artifact directory: `crew-ui-final-clock-proof/`. The earlier failed original and the deliberately reproduced failure remain preserved alongside passing diagnostics.

`crew-ui-clock-review.json` contains compact checkpoint comparisons, actual source hashes and comparison against runtime checkpoint789a616. These isolated Chromium D3D11 runs use a controlled functional clock; no rendering performance, human play or gate approval is claimed.

Freeze verification: all four original/diagnostic/fixed scripts have no git diff against789a616. Worktree CRLF versus committed LF explains raw-byte hash differences; their normalized contents are identical. No source or script edit was made after the final proof began. Only this report and its comparison JSON were added afterward. All owned test browsers are closed.
