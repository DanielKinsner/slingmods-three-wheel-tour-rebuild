# Bounded independent helper review

One read-only helper examined installed Three.js 0.186, the changed lighting/preparation code, baseline GL timings, the first repaired full1080 run and all six luminance sweep candidates plus three streetlit frames. It made no source edits and ran no browsers or tests during scored work.

The helper found two preparation issues: attachment could revert to an obsolete equipped state across an await; the bay loading veil began after its first render. Both were repaired and the helper confirmed the current-state restoration, early veil and cached immutable statistics. Unit coverage exercises equipped state changes during preparation.

Independent calculation from cycle1-full1080 reproduced 9,410 running intervals across two valid 78.402539-second laps: median/p95 16.7 ms, p99/max 16.8 ms, no intervals above 33.4 ms, 14 resident programs and zero buffer overflow. This was an intermediate application build, not the final repeated trial matrix.

The helper judged 900 renderer nits the balanced candidate: red/cyan distinguishable at both inspected lighting conditions, less weak than 300, less spread and near-white cyan than 1,800. It recommended no further lighting iteration. Those were stationary near-chase diagnostic frames, not motion, far-chase, daylight or fine-occlusion proof. The lead reviews the final separate evidence and Astra remains approval authority.
