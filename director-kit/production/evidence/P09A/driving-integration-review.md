# P09A driving / integration independent review

## Scope and identity

Read-only source review of runtime `111f3aae5e1cbff1ac46082117b6557feced5cbf` in the current feature checkout. Reviewed Express integration, SceneLifetime, preparation jobs/renderer, entry timings, recovery detection/policy and held-input handling. This pass ran no browser or CPU simulation jobs while the lead's native timing matrix was active. The earlier five recovery tests are separate test evidence, not a new runtime benchmark.

## Repairs verified in source

- **Loading exception lock:** showroom action catch now clears `pending`; failed dialog setup removes its DOM node before rethrowing. Preparation failure can return to usable showroom controls.
- **In-flight lifetime:** SceneLifetime registers pagehide ownership at module start. Renderer, outdoor resources, hero, route presentation, products and physics are owned as acquired. A resource resolving after departure is immediately disposed and rejects with AbortError. The Express RAF stops when pageActive is false. This addresses the earlier late-only cleanup registration.
- **Preparation retry overlap:** a failed job aborts its batch and awaits allSettled before offering retry. Old workers cannot overlap the next batch or contaminate its resource counts.
- **Cup reset identity:** canReset now detects a valid finish before restart dispatch; once saved it navigates to the career hub, and before saving it rejects reset. It cannot start Cup stage1 while retaining stage0's loaded route.
- **Unsaved-result navigation:** menuAction blocks ordinary leave/retry on valid-but-unsaved results. Continue/Back and the route link are disabled/hidden until receipt completion. A failed save has explicit Retry and Leave without this reward controls; leaving is not presented as a successful save.
- **Held keyboard after free respawn:** held keys are copied into releaseKeys before clearing the KeyboardBuffer. The player must release controls after respawn, consistent with scored reset. Gamepad rearming remains InputResolver-owned.
- **Timing names:** EntryTrace now documents that firstControllable is the rendered ready menu / enabled start controls, and separately records firstDrivingTick after starting/countdown. Wall-clock stages are not labeled GPU timing.

## Remaining actionable finding at reviewed commit

**P2 — paused result waiting cannot resume through its visible primary control.** If the player finishes validly before the remaining field, blur pauses the world while the result waits for all finishers. `menuAction` rejects `resume` for valid-but-unsaved results, and presentation changes the paused primary to disabled Continue chapter. Mouse/controller confirmation cannot resume the field, so saving waits indefinitely; Escape is an undisclosed workaround. Permit resume through that guard, and keep the paused menu's Resume control enabled rather than overriding it as Continue chapter until unpaused. This was sent to the integrator immediately. It requires targeted validation after repair.

## Recovery tests authored earlier

`tests/p09a-recovery.test.ts`: five passing tests covering reason precedence, moving/airborne false-positive boundaries, six-metre rearward road placement, start-line wrapping, immutable inputs, and an **actual Sport v2 high-speed-curb simulation** that overturns, is detected and returns to supported upright state after explicit free recovery. The only initial test failure was strict +0 versus -0 yaw comparison; the repaired assertion checks numeric angular equivalence. No runtime correction was needed for that assertion.

## Limits retained

- Scored recovery creates a fresh attempt at the grid; free preview alone relocates to the road. No checkpoint advance or reward proof is generated by relocation.
- Deliberate full-speed curb stress can overturn v1 and v2; the respawn is explicit arcade recovery, not invincibility.
- Source ownership review does not measure GPU memory or prove all driver-level resource release. Browser/GPU/performance/career persistence evidence belongs to the lead's independent integration runs.
- Existing fidelity, G3/G4, physical-controller breadth, human driving/listening, destination-device and release/publication holds remain.

Status at111f3aa: named earlier defects repaired; one result-wait resume edge case remains as above. No other bounded blocker identified in this source review.

## Closure — source audit at049c1cd

Reviewed the full `src/` diff from111f3aa through **049c1cdd154f4ca83d087f99e0dad76040c049f7**, and confirmed there were no additional uncommitted source changes during this check. This closure supersedes the earlier source-level pending finding, while preserving that historical finding above.

- **Result-wait Resume repair is present.** `menuAction` permits Resume while a valid result awaits its receipt. The primary-button override now excludes the paused state, preserving the existing enabled paused-menu Continue action. The earlier mouse/controller deadlock is resolved by source inspection. This review does not claim the targeted actual-finish/blur/resume browser proof; that is a separately running integration test.
- **Sport rival parity is restored.** Trait scaling now applies to both Sport versions rather than v1 alone. Pace/lateral/headway retain the established v1 factors; no extra v2-only AI bonus was added. Earlier v2 career runs using the unintended legacy-paced planner remain pre-repair evidence. The focused regression previously passed across Maya, Jett, Nico and the player planner,15m/12m road widths, six straight/curve/braking positions and repeated planning ticks. It checks identical v1/v2 target speed and control output, distinct legacy pacing and no vehicle-state mutation. No test was rerun during this final review.
- The previous lifetime, retry settlement, cup reset, held-input and loading-error repairs are unchanged. No new bounded correctness blocker was identified in the reviewed source diff.

### Performance remains HOLD

I read only the small final-runtime native summary files; I did not rerun races or independently recompute the complete raw timing arrays. Those summaries identify049c1cd and contain16 valid player results and64 finishers.13 attempts meet every timing threshold; three fail the unchanged100ms maximum:

| Current native case | Attempt | Maximum active-race frame |
| --- | ---: | ---: |
| Express equipped,1280 profile | 1 | 200.0ms |
| Express stock,1280 profile | 1 | 133.4ms |
| Harbor equipped,1920 profile | 2 | 183.3ms |

These failures remain retained and **performance HOLD**. Functional completion, passing percentile values or the other13 attempts do not erase the maximum-frame failures. No source-review conclusion here upgrades performance, hardware, human-fun/listening, fidelity, G3/G4 or release approval.

Final source-review status: named correctness findings closed in current source; targeted integrated finish/recovery evidence belongs to its own receipt; performance HOLD remains explicit. This pass used only source/diff and small-summary reads, and changed only this review document. No browser, simulation, native benchmark, commit or push was performed.

## Bounded finish-wait/resume closure — 049c1cd

Two isolated control-only driving attempts were run against the frozen static build at port5202; no production state or physics code changed. Both failures are retained. `finish-wait-resume-01` achieved a valid **second-place** player finish (78.898s versus Jett77.741s), so the required finish-ahead waiting state was not reached. `finish-wait-resume-02` used the one permitted adjusted input planner; it entered physical recovery and requested reverse, which the existing evidence adapter deliberately rejects. The attempt stopped. Their exact planner settings and limitations are in each folder's `CONTROL-PLAN.json`.

**Actual finish-ahead → pause waiting field → Resume → reward remains unverified by this targeted browser proof.** No runtime defect is inferred merely because these evidence drivers failed to finish first. Source inspection establishes that Resume is allowed and its paused primary button remains enabled. `tests/p09a-career-ui-state.test.mjs` separately **passed** a clearly synthetic UI fixture: finished player plus still-running Jett, real pause-button click, enabled Resume action and controller activation. The synthetic UI test does not certify the full runtime/award path.

The complete ordinary gameplay loop `career-loop-08` remains passing on the Sport-paced candidate. All worker browsers are closed; there will be no further tuning attempts for this scenario in this assignment. Existing performance and approval holds remain unchanged.
