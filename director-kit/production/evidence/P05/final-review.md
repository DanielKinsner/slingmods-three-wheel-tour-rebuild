# P05 integrated first-chapter review

Runtime: `7fe1e881b0712be2fa09b4c4eec6baa9d0ebb0d6`, existing rebuild on main. Runtime source, tests, capture harness and selected public assets match `build-inputs-verified.json` throughout final verification. Packaging-only reports and diagnostic tools are separately hashed in PACKAGE-MANIFEST.json. This is a playable first chapter with a held performance finding, not a broad G3/G4 approval.

The root bay now carries Rae's invitation through the existing clean-lap/optional SM-133 loop into a two-lap night race with Maya, Jett and Nico. All four cars inhabit one Rapier world, step together, have independent drivetrains and dynamic collision response, and pass the same ordered lap gates. Three production controllers issue ordinary controls; they never place a car on a desired pose or assign a finish. Finish order uses common time and fractional crossings. The player can retry immediately with a new attempt ID or return to the saved build. The original day/night solo events and their records remain separate.

Safe v1-to-v2 career migration, atomic receipts, chapter completion and save failure handling were tested against real isolated IndexedDB. Fresh flow earned 800 on a real clean lap, optionally spent 600 on the existing kit, finished third for 150 plus the one-time 400 podium bonus, and reloaded with 750 and cyan installed. Existing earned-v1 flow preserved its 300 and cyan, reached 850, and reached 1000 after a second third-place result without repeating the bonus. These are isolated fixtures; no personal browser data was accessed. Invalid results earn nothing. Purchase is optional for crew eligibility.

The full suite passes 102 tests. Solo before/after parity matches all 4,200 full telemetry objects, SHA-256 `7a6c43ce2c55c97c4f9a21997179ee5d0f712b957da13209acd5a19222fd8696`. An actual sensor-plane support test matches all 180 telemetry frames against its control. Protected-source/asset checks retain all 88 preexisting public assets and 120 protected files in total. Two current full-field physics runs use distinct seed/start/input fixtures and retain complete compressed per-tick telemetry. Blocked line, side-by-side and light-contact cases exercise actual dynamics. Normal current runs finish all four without hidden resets or retirements.

The run repaired defects discovered during integration: mutable rival-resource ownership, lifecycle/menu handoff, pending-save reset protection, denied-storage crew navigation, replacement-controller arming and recovery UI hidden behind the loading veil. A native race exposed Maya's full-throttle response to a fast sideways slide, compounded by evidence-player tailgating. The production controller now uses ordinary brake/steer to recover that measured state; player physics constants are untouched. The evidence driver was separately repaired, and the harness now asserts every attempt's field, not only its final retry. The original failed trace is retained with a causal-facts/inference note in `native-contact-repair.md`.

Final native performance is reported without video overhead in `performance-review.md`. The original matrix is mixed: one stock720 active interval is 366.6ms, with 266.6ms clipped by the unchanged catch-up cap; the original equipped1080 repeat misses p95/p99 targets at 33.4/50ms. Clean followups and a separate trace did not explain the historical pause. Resource counts stayed stable. The trace's racing GC maximum of 5.770ms is not evidence that GC caused the historical pause. No speculative optimization or resolution reduction was substituted for diagnosis. P05-18 performance acceptance remains held; an exact continuation is in the root `AUTONOMOUS_RESUME.md`.

The 199.12-second continuous runtime video is a real ordinary wall-clock two-lap race, ending third with all four valid, then returning to the preserved build. It is explicitly SILENT: the isolated recorder did not capture audio. The actual shared audio context, decoded bank, at-most-two opponent slots, RPM/mix/attenuation and mute/volume/pause paths were tested separately with real signal measurement. No human listening approval is claimed. The extra 18-second flow is prominently labeled edited runtime stills from controlled-time functional testing, never performance proof. See `media-review.json` for hashes, inspection limits and camera events.

Separate bounded source reviewers examined competition and career/integration; their notes disclose ownership and the build they examined. The lead integrated their repairs and ran final runtime/media checks. This is not an independent external certification. Physical-controller feel, foreground display responsiveness, other GPUs/mobile, human fun and audio audition were unavailable. Visible debt includes the sparse harbor, provisional materials/driver, close-range headlight wash on rival bodies, simplified instruments/mirrors, synthetic engine sound, and legacy bay candidate/footer navigation labels. No new asset modeling, opponents beyond the three specified rivals, products, tracks, Chapter 2, spending, deployment or desktop takeover occurred. G3/G4 remain pending.
