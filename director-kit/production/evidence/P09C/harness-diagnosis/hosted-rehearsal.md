# Hosted sequence rehearsal

The first rehearsal held throttle immediately after Start, before the existing neutral-input arming boundary. The car correctly stayed still; this was not accepted as a driving pass. The repaired test waits until the real countdown completes before pressing throttle, and explicitly restores focus after closing the external product popup. Its movement assertion, real full-race validation, pause, retry and same-build assertions all pass in hosted-harness-local-02. The actual hosted URL remains a separate required run.
