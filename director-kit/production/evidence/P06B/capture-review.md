# Capture configuration correction

The first stationary final-visual attempt omitted SHOWCASE=full and displayed the retained old physical harbor scenery. Detected by checking the unusually small inspector JSON and the missing showcase binding. It is retained locally, explicitly rejected, and excluded from the ZIP. Production crew/solo always load the full showcase, so the ordinary runtime tests/films are unaffected. The accepted replacement is final-visual-02, requiring full-stage runtime material bindings and frozen source/asset hash equality. No game-source change was needed.

The original daylight virtual-input attempt also omitted the guarded test token after ordinary scene navigation. It timed out at150000ms without a lap and is rejected; its silent output is retained only locally. The separate corrected capture tool is hashed in capture-tooling-amendment.json. This repairs the harness rather than bypassing the production guard or changing game logic.
