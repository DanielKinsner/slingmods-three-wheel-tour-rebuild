# Departure critique and repair

The first real camera interpolation cropped the front of the car. The repaired shot follows the car until it moves, eases to a 46-degree field of view, and keeps the camera inside the showroom. Inspected opening, rollout and returned-build stills at 1280x720 have readable controls and no vehicle cropping. The existing driver appears for departure and the saved driver preference restores on return. The final car center is X7.8, keeping the full vehicle on the authored apron.

The 5.8-second transition is presentation animation. It does not step or modify simulation, race results, products, earned ownership or saved recipes. Door rolling is approximated by translating and compressing the authored ribbed curtain. No exterior destination is added. Natural completion and three skip controls navigate only once. Pausing and browser focus loss freeze movement; reduced motion bypasses the transition.

All six integrated navigation checks passed with the saved recipe and career sentinel preserved. Cue loading during pause stays stopped at offset zero. For a previously playing cue, paused source playback stops and its offset remains fixed. The AudioParam retains its last DSP value after the branch stops; evidence keeps that value alongside gainTarget=0 and playing=false. Root owns the separate live audible capture.

Earlier incomplete/failed harness artifacts remain under showroom-departure*, including the missing test query on the controller-return case and missing read-only reducedMotion inspector. Those harness gaps were repaired. The initial stale-gain assertion was diagnosed rather than hidden. This evidence records working-tree source hashes; final static build and native film identities belong to the integrator's report.
