# Codex assignment — Astra Director Review07
## P04B1: Make It Yours

Continue the **existing correct rebuild**:

`C:\Users\SM - Dan\Documents\GitHub\slingmods-three-wheel-tour-rebuild`

Read `AUDIT.md`, `packets/P04B1-MAKE-IT-YOURS.md`, `references/PRODUCT-SOURCE.md`, and `ACCEPTANCE-CASES.json`. Execute the packet, not another proposal. Make ordinary creative and engineering decisions without handing them back to Dan.

## Authority and scope

Astra accepts the specific rear clearance/articulation repair for continued development. Existing vehicle fidelity, the raycast/linkage approximation and environment/aural/hardware holds remain. P04B1 is explicitly authorized while G3/G4 are pending; do not interpret the old upgrade deferment as blocking this new assignment. Do not mark global gates passed.

Deliver **one complete real-product loop**: valid Harbor lap → earned game credits → return to the existing bay → preview/purchase/equip TricLED base RGB underglow SM-133 → choose color → drive the same harbor at night → reload and retain the build → intentionally open the real product page. Include the short new story beats in the packet. No new opponent, track, vehicle, performance tuning or full campaign.

Freeze the repaired car geometry, rear rig, driver, contact positions, tire physics, route/colliders, race-validity math and existing sound bank/mapper. Add the lighting hardware as a separate Blender-authored accessory asset with a small attachment descriptor; do not rebuild the car to mount it. The only allowed bay changes are the compact product interface, sensible camera framing and a local lighting-preview mode. No new showroom architecture or environmental pass.

## Safety and working method

- Confirm exact root, HEAD and status. Last reviewed runtime is `fd59df31f9f03f168b6a93e26d48c296526e1b53`; packaging is `b51c846c173f4bdfd9772d265e80e0b472f66dac`. Do not reset to either or overwrite newer work.
- Preserve history and current working state. This kit is instructions/evidence, not a source overlay. Append decisions to the real current ledger; the prior ZIP's `state.json` was a labeled excerpt and must not replace that ledger.
- Use one lead and, only when useful, one bounded artist. Do not burn the remaining session on an expanding reviewer swarm. Two evaluated accessory/lighting revisions maximum before returning the best honest state. Use the existing isolated tools and installed Blender; never take over Dan's desktop, mouse, browser profile or Blender UI.
- No external spending, paid downloads, API generation, service accounts, live store/cart writes, credential requests, push/deployment or public uploads. Read-only primary-source reference work is permitted. If a reference or hardware test is unavailable, disclose it rather than fabricate it or make Dan gather it.
- Continue to build and test as you go. A test count, receipt ledger or pretty garage image is not evidence that the loop works through the actual menus.

## Return Astra-Review-08.zip

Preserve relative paths. Include current source/config/lockfile/tests, new authoring and capture scripts, all public assets required by the captured build, the editable new accessory `.blend`, and any source dependencies needed to reproduce changed assets. Do not duplicate obsolete media or whole historical kit archives. Existing unmodified Blender sources remain in the repository; they need not be recopied solely to inflate this review.

Include:

1. `REVIEW-ME-FIRST.md`: actual root/HEAD, what changed, launched default path, controls, implemented/held distinctions, first-completion/replay amounts, balance and inventory outcomes, exact product configuration, reference/approximation limits, and test/runtime environment.
2. Package SHA-256/size manifest; captured source and served asset hashes; clearly separated packaging-only commits. Hash preservation of frozen current car/rear/contact/driver/route/physics/audio inputs against Review07.
3. One actual UI progression recording: start on an unmodified save or disclosed fresh local fixture; valid result and reward; return to bay; preview; cancel once; purchase; choose a second color; stock/installed A/B; exit preview; start night drive. The valid race and its UUID can be documented by the companion full-lap trace rather than edited to imply a new run. Disclose any edit/cut, virtual controller, controlled clock or offline audio render.
4. A full valid night lap with the installed part through ordinary input/presentation, plus its logical telemetry and result. No transform teleporting or hidden test-only equip state. A 10–15-second **wall-clock** night excerpt/profile is separate from controlled-clock evidence; record hardware and limitations.
5. At most eight purposeful stills: compact garage, kit mounting detail, matched stock/installed night A/B, second color, daylight installed view, a moving corner, and persisted build after reload. Use normal opaque bodywork. No Blender beauty image substituted for runtime.
6. Browser evidence for reload, old-save record/settings preservation, duplicate reward/result, insufficient funds, repeated purchase, two-tab contention, blocked storage fallback, preview cancel, external-link behavior, and keyboard/controller menu access. External link tests must not add items to a real cart.
7. State-transition receipts sufficient to follow `0 → 800 → 200` credits on the first loop and one replay's `+100`; stable attempt IDs and once-only chapter flag; truthfully labeled fixture vs ordinary-action evidence. No real dollars or store reward points in the game wallet.
8. Full local tests/build and focused regressions; unchanged physics trajectory check with cosmetic kit on/off; before/after frame/draw/resource measurements at matched scene/viewport. Give missing checks explicitly.

Never include .git, node_modules, downloaded tools, caches, dist, .env, credentials, or redundant historical captures. Keep the original project/saves/deployments untouched. Create a numbered ZIP if the intended filename already exists. Return its exact path and size, then stop for Astra's review.
