# Codex next assignment — Director Review06

You are executing Astra's bounded production decisions. Continue the SAME rebuild, not the old project and not a new repository:

`C:\Users\SM - Dan\Documents\GitHub\slingmods-three-wheel-tour-rebuild`

The owner submitted Review06 and then noticed the rear tire/swingarm/shock occupying the body/storage area. Astra confirmed **actual exported rear-tire geometry inside the solid rear firewall**, plus missing visual articulation of the rear arm/axle/shock. The exact measured collision is in `evidence/rear-assembly-diagnostic.json`. Read `AUDIT.md`, `REFERENCES.md` and both packet files before editing.

## Decisions and priority

1. Preserve the accepted event, audio-RPM correction, front repair, driver, controls and physics. No restart or broad rework.
2. Execute `packets/P04A1-REAR-ASSEMBLY.md` FIRST. This overrides earlier “no more vehicle modeling” instructions only for the rear clearance/body-storage/suspension assembly and shared presenter necessary to fix it. Do not repaint/remodel the front or cockpit.
3. A separate artist may perform `packets/P04A2-WATERFRONT.md` while rear verification proceeds. With one worker, finish rear integration before that secondary pass. No shared-file races; the lead owns final asset references and integration.
4. The previously discussed upgrade/reward/catalog loop is DEFERRED. No additional product, story, currency, AI rival, vehicle, circuit or deployment. This exception must not silently expand into the original full P04.
5. G3/G4 remain pending. Local repair evidence may pass; do not promote global gates.

## Start safely

Confirm the explicit repository root for every command. Record HEAD/status and preserve all owner/newer work. Review06 captured runtime was `e1fafb5ec4d371635bfbeca51663fc67734a40ca`; do not reset the working tree to it. Use the current tree and document intervening changes. Make a scoped safety commit only when it does not sweep in unrelated owner work; otherwise use the existing clean recovery convention. Never reset/clean/delete the previous game.

The supplied kit is guidance/evidence, not a patch to blindly overlay onto project source. Do not replace the project's production ledger with this kit. Append the new packets, blockers and evidence to the existing ledger. Keep prior claims and evidence historical.

## Execution style

Make routine art and engineering decisions. Use isolated/headless tools; never take over the owner's mouse, desktop, browser profile or interactive Blender session. Use installed Blender/local authoring and existing tools. No paid asset downloads, new service subscriptions, API spend, uploads, secrets or storefront writes. Read-only reference research is allowed. A numerical diagnostic is not a visual reviewer; review the exported result in the actual runtime.

Read the rear packet's kinematic approximation warning. Preserve the contact centers and tire physics; do not move the rendered rear wheel off its telemetry to hide an intersection. Do not simply hide the shock, cut away needed storage walls, enlarge the vehicle or make panels transparent.

## Return package: Astra-Review-07.zip

Keep relative paths and include only current required build files and a curated evidence set. Exclude node_modules, tools/Blender downloads, dist, caches, backups, .git, secrets/.env and redundant historical captures. Include:

- `REVIEW-ME-FIRST.md`: exact root/HEAD, launch commands, implemented/held items, found cause and actual fix, test environment and limitations, owner screenshots addressed, and whether either local pass is held.
- Current `src/`, relevant configs/manifests/lockfile, tests, authoring/capture scripts, public assets and editable Blender sources needed for current operation and the corrected rear/harbor. Preserve the rest of the existing checkout.
- Root package manifest of file SHA256/size; captured commit and source/served-asset hashes. Every shown build must be attributable. A later package-only commit should be labeled.
- Rear daylight rest-pose images: matched left/right side, directly behind, three-quarter and one underside/cutaway diagnostic with wheel center, arm pivot, shock endpoints and storage envelopes. Diagnostic colors/overlays must be labeled and not used as the beauty result. Include a normal enclosed-body view in the same pose.
- A 12–20 second actual exported runtime rear-motion recording: rest, supported bump/droop, wheel rotation, normal braking. Separate deliberately injected diagnostic poses from normal simulation. Supply center/attachment/clearance telemetry and any small visual-link approximation error/stretch.
- A full valid day lap (ordinary input/presentation; sound aligned if supported), a 20–30 second night excerpt from a fully logged valid attempt, result/retry proof and a short rear/bay orbit. Virtual input or controlled-clock capture remains allowed only when disclosed. Do not call it a human or physical-controller test.
- Matched current waterfront day/night views showing the three named environmental changes without changing route data. Report actual material/draw/resource differences; no hardware claim from asset counts.
- Full local tests/build logs plus focused new rear checks and the existing 26-test paths. Record tests not available instead of pretending they passed. Give a summary of source/asset files deliberately allowed to change and protected comparisons at mesh/component or source level.

If a blocker survives TWO local evaluated geometry revisions, retain the best safe state and return an honest HOLD with the specific failed case. Do not loop indefinitely or silently cover it up; finish any independent secondary work already authorized. Never mark the rear complete while the known rest collision persists.

Create a numbered ZIP if the output name exists. Return its exact full path/size and stop for Astra's review. Do not ask Dan to choose the modeling method, find reference photos, debug Git, or run your QA.
