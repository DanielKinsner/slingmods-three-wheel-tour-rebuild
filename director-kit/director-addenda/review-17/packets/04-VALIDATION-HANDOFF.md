# 04 — Integration, proof, performance and handoff

## Preservation and correctness

Keep Sport-v2 braking, original diagnostic profiles, guard/floor policy, reverse/recovery and route geometry unchanged unless a specific new reproduced defect needs a narrow documented repair. Protect all current owner front/grille/hoop changes, products, sources, finishes, showroom construction/door/departure, earned ownership and saved builds.

Retain schema4 migrations and result bindings, duplicate/abandoned reward protection, Cup leg continuation and entry-build capture. Free preview never changes earned credits/ownership. Preview/install sounds do not signal an earned purchase unless that transaction succeeds. Free and owned-equipped builds should render and sound consistently for the same recipe, while preserving separate state.

## Targeted frame-stall work

Review17's clean native matrix has3 failing attempts/8 active frames over100ms (max200ms) and predates latest art. Establish a current full-asset baseline, then profile the newly integrated candidate. Separate UI, JS/GC, physics, GPU submission/work, audio decode and asset preparation. Inspect repeated telemetry allocations, unchanged HUD writes, screen uploads and sound creation as hypotheses. Existing change-key caches must not be misreported as absent.

Use traces only to investigate; run uninstrumented native comparisons separately. Preserve every sample, ready/loading interval, failed hypothesis and both first/retry races. Don't improve a statistic by removing spikes, racing less of a route or quietly reducing resolution/rivals/lighting/material quality. Don't kill unrelated desktop processes to obtain flattering results. Prove changes with matched runs, not an unsupported “host load” explanation.

Default UI numerical updates can be decoupled from render rate with cached node references/diffed values; physics/camera/render remain coherent. Dynamic screen textures and audio buffers must not be rebuilt each frame. Bound cache/resource lifetimes across repeated signature→departure→drive→return transitions. Resource counts are not measured VRAM.

Minimum final native matrix: same eight720p/1080p × Harbor/Express × stock/equipped configurations, two complete attempts each, audio graph enabled, latest final art and display. Use existing thresholds p95≤20ms, p99≤33.4ms, max active interval≤100ms with explicit numerical tolerance only. UI stress and profiling runs are separate. Account for audio/UI/cockpit feature cost with appropriately matched additional cases. Record CPU/GPU/browser/DPR/tune/build, source/assets and exact runtime.

A remaining reproducible failure is an explicit HOLD, not an all-pass. Complete useful UI/audio/display work and local diagnosis inside the run, then return the best integrated candidate and precise remaining blocker if bounded approach changes still fail. Do not initiate an indefinite unrelated optimization/engine-migration project.

## Tests

Run full source tests and both builds; extend focused tests for successful-transaction cues, duplicate/no-op actions, preset batching, decode cancellation, volume persistence, unlock/mute/blur/pause, sample offset resume, scene disposal and offline operation. Test the actual equipped Thermal exit plus stock/non-equipped negative case. Ensure source/capture mixing routes include SFX and sample playback.

Exercise showroom navigation and resize, text overflow, focused/disabled/locked states, all products, long names, user presets, protected career save state, full chapter/Cup path and return. Recheck the early-player-finish/rivals-still-racing pause/resume edge with actual production input and distinct labeled synthetic regression proof when natural occurrence is not attained. Don't inject a finish and call it a real race.

Check dash on/off independent from audio and underglow, reverse display values, interior/cockpit readability, no offscreen/distant texture-update waste and no material/UV regressions around the bezel. Checks that a file exists do not establish visual quality.

## Final media

- Normally10–12 carefully selected matched screens spanning configurator, career, HUD/results/settings, UI scaling, and actual powered display. Keep necessary ordinary views; don't submit only flattering closeups.
- One3–4-minute current-runtime film: first entry and category/preset changes; shock installation clink and another material cue; dash power; owner Thermal-equipped cinematic exit; drive/brake/shift/reverse; race/result/reward/garage return. Capture the actual game master mix, not an added soundtrack. Mark edits, automated input, version and any sync diagnostic. No silent workshop labeled sound-enabled. Don't substitute older art footage.
- One small level-matched engine A/B demonstration with source/tune/state labels; retain lossless source excerpts in Git. Avoid a huge collection of raw movie duplicates in the ZIP.
- Complete relevant raw native timing/verification, including failures and second races, losslessly compressed when needed. Include model/cue/sample metadata, API usage count/allowance decision with secrets redacted, missing-source list if any, and disclosure of listening capability used.

## Git and lean upload

Commit and push all required source/config/pinned deps, current editable art/display sources, original supplied WAV, baked generated/processed sound bank, safe provenance/rights notes and handoff to the existing private feature branch. Local runtime must require no ElevenLabs key or online-generation connection. Keep secrets/personal browser saves/tool installs/node_modules out. Runtime-generated output can stay excluded when the remote clone reproduces it from tracked inputs.

Prove recoverability in an isolated fresh clone FROM THE REMOTE, not a local worktree or folder copy: install, tests, build/demo-build, current asset/audio dependency hashes and relevant browser smoke flow. Never copy missing original-machine assets to make it pass. Locate tools/paths portably. Distinguish this same-host recovery proof from destination-device testing. Record exact tested runtime, packaging and receipt-only follow-up commits. Verify the final remote tip; preserve unrelated local edits. No merge to main or publication.

Return **Astra-Review-18-Lean.zip**, target60 MB/hardcap100 MB. Include current small source/tests/scripts, audit and focused media/evidence, not full historical backups or unchanged meshes/texture masters. Identify omitted heavy assets by exact remotely retrievable commit/path/hash. The ZIP is not a self-contained build.

Final user message: what changed, exact local launch/address and stop command, ZIP path, runtime/branch/pushed SHA, evidence limitations and one clear next action for Astra. No routine tasks pushed onto Dan.
