# Astra Director Review 03

Continue the existing rebuild. **Do not start a new project.**

Read `CODEX_NEXT.md` for the next implementation assignment. It expressly authorizes two small, independent work streams: a bounded front-shell repair (P03A2) and input/chase-camera/practice-route work (P03B1). This is a director change to the previous strictly sequential plan, not a visual PASS.

`AUDIT.md` explains the decisions and verification limits. `FRONT_SHELL_SPEC.md` identifies the local construction issue. `evidence/` contains the independently executed regression results, archive/asset inspection, source diff, and selected supplied runtime views.

**G3 stays pending.** Keep the current vehicle as a provisional development asset unless the local front replacement earns acceptance. Do not reinterpret this packet as permission for a new vehicle, campaign, large environment, engine rewrite, full P03B, or production deployment.

Return `Astra-Review-04.zip` to Dan, then stop. He only needs to upload that ZIP to the same chat.
