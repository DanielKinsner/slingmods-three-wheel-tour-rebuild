# Validation and delivery / one useful Review21

## 1. Evidence should answer the owner's question
“Does this now look and feel closer to the cinematic references while remaining our actual game?”

Keep validation proportionate. The result is not a larger archive of reports with tiny visual changes. Use the established test/recording infrastructure and protect original evidence; add targeted coverage for the new UI and room. A screenshot file existing is not a visual acceptance test.

## 2. Visual acceptance
Capture both a matched before/after camera and the new intended hero composition. Same finish, products, viewport and mode for matched comparisons; record any intentional camera/exposure difference in hero comparisons. At minimum inspect:
- entry with original set visible, plus an alternate view showing the mural on the chosen blank side wall;
- build Paint and Lighting, Exhaust and long Suspension content, all four finishes and both lighting modes;
- destination screen selecting each of the three routes, including correct Ridge late-afternoon/blue-hour response;
- actual HUD at speed on Harbor/Express/Ridge, including cockpit and a bright scene;
- actual result for a finish, a non-winning result and an early-player-finish waiting state;
- career/shop, saved build, audio/settings, load-failure recovery and keyboard/controller focus.

Use 1280×720 and 1920×1080 for full visual proof; 1366×768 and 2560×1440 for layout checks. A narrow-viewport smoke test is not mobile gameplay certification. Avoid clipping, accidental horizontal scroll, unreadable labels, duplicate controls and giant opaque panels stealing the road. Prove selected state with more than just color.

Supply one compact contact sheet with annotated remaining differences, not dozens of almost identical images. The readable reference markup is not runtime evidence and stays clearly labeled.

## 3. Functional preservation
Run the complete established test suite and both ordinary/curated builds. Test the real UI hooks, not a separate mock page. Preserve substantive regressions while updating selectors only where the UI refactor requires it.

Exercise free preview → each product/finish → compare → undo/save → destination/time selection → same-build drive → pause/retry → return → load saved build. Confirm no free preview spends career credits or rewrites existing receipts. Existing fitment checks and all options remain. Native storage-unavailable and async cancel/failure paths retain clear recovery.

Cover all current chapters and Cup continuation with the established fixtures/tests; complete at least the affected actual race/results paths. Results animation cannot issue rewards twice, grant a false podium, break early-finish pause/resume or reset old saves. The mount/driver/display/departure pipeline remains intact. Single clicks produce one successful-action sound; selecting a preset, loading a save and repainting the UI do not fire a volley of installation cues.

Showroom resources: orbit, change finishes/products repeatedly, open/close inspectors, leave and return twice, and fault an optional mural/preview request. A mural failure should not destroy the saved build; dispose partial loads and provide a sensible fallback. Do not retain an expensive second world just to display a thumbnail or result backdrop.

## 4. Performance and visual-cost isolation
Keep baseline hardware/resolution/pixel ratio/quality documented. Measure native frame intervals without video capture, devtools tracing, compression or Blender work concurrently. Report CPU/GPU/renderer and settings. Preserve all outliers and failed runs; loading/ready intervals are distinct from active racing, not erased.

Retain the current working race thresholds: p95 ≤20 ms, p99 ≤33.4 ms, maximum active interval ≤100 ms (with only the established floating-point comparison tolerance). These are project targets, not a universal performance certification.

A useful targeted matrix is two complete consecutive races on each of Harbor, Express and Ridge at 1080p using the new HUD; include both Ridge light conditions across the matrix and at least one real cockpit race. Include stock/equipped coverage and one 720p fallback run. Expand only when a change or failure requires it. Do not rerun dozens of redundant routes just to fill the report.

Separately measure showroom cold entry, orbit, option changes, material warm-up and return. Record its larger effect costs explicitly rather than hiding them behind good race numbers. If optional reflections/postprocessing miss the budget, fix/reduce that feature, not the whole visual ambition. Keep a usable quality fallback and label what differs. Renderer object counts are not measured GPU memory.

Do not claim the old driving feel was human-approved because a test passed. No retuning Sport v3, brakes, physics, grid/route or rewards in this assignment.

## 5. Git and hosted completion
Continue the existing owner's main workflow. Inspect current status before integrating. Commit source, current editable wall/room assets and dependencies, runtime derivatives, license/provenance, export scripts, typography inputs when lawfully sourced by the local project, test changes, director packet, selected current evidence and the portable handoff. No credentials, browser saves, downloaded tools, personal metadata or build caches. Keep original reference photos and their usage appropriately separated from served assets; do not add full concept screenshots to production output.

Update the actual runtime asset closure and curated/Vercel build path, including the new wall texture and any font files acquired by the local project. Deploy only the verified curated runtime. The existing canonical project is `slingmods-three-wheel-tour-rebuild`, project ID `prj_3UBN81UQim5W3obOGNHc9wpfNTDY`; resolve current account/project settings instead of creating another one.

Run a fresh remote-clone build/asset recovery verification without copying ignored data from the original machine. Disclose shared package/browser caches and same-host limits. Use an immutable runtime identity, a separate packaging identity, and a final receipt; do not rerender merely to retrieve a ZIP or claim a metadata-only commit is new gameplay.

Test the actual HTTPS canonical root with a fresh context. Confirm the new screen/wall is served, actual runtime identity/assets, all three destination entries, a complete race/retry and same-build return, one deliberate real product link, and excluded source/ZIP/editor paths returning 404. Keep current repository visibility unchanged. If pushes auto-deploy, stage recoverable local work and push the intended validated candidate; don't intentionally publish broken intermediate design experiments.

If deployment access fails, record the precise returned error and preserve the complete local/Git candidate. Never claim READY equals tested gameplay or invent a hosted URL. No account upgrades, new sites or charges.

## 6. Return package
**Astra-Review-21-Lean.zip: target 60 MB, hard maximum 100 MB.** Include:
- START-HERE / summary with runtime, packaging and remote identities, source changes, current launch/hosted URL and honest limitations;
- current small source/scripts/tests/config/lockfile plus needed manifests, not only a cherry-picked prose summary;
- roughly 10–14 useful compressed stills, including matched comparisons, wall placement and responsive UI;
- one roughly three-minute edited tour of entry/orbit/mural/configure/destination/HUD/results/departure with captured game audio; disclose edits and automation, keep at least a continuous driving segment;
- complete relevant raw performance and functional data, including failures, compressed sensibly;
- an exact hashed inventory of heavy omitted art, editable sources and original capture locations recoverable from the verified Git commit.

Keep the full-resolution artwork, editable sources and original evidence in Git, not duplicated in every review ZIP. Keep reference concepts labeled as concepts. Do not put an old pre-change film in as proof of new visuals. Include audio stream/sync checks but do not treat those as listening approval.

Commit the finished lean review ZIP and its portable receipt to main, following the current owner policy, without recursive hashing or rewriting original evidence. Return a short human-readable summary with the actual file/path, latest main SHA, tested runtime, hosted verification result and any one true blocker. No requirement for Dan to gather new references or run routine QA.
