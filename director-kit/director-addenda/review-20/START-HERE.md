# Review20 → Review21 / P10B — Cinematic Identity
**Prepared for Dan by Astra · 16 September 2026**

This is the actual implementation packet: the master assignment, named image references, a generated Tour Wall demonstration, detailed UI/rendering directions, a scoped CSS seed, and delivery requirements. It does not contain an implemented game update.

## Start
Give Codex `KICKOFF.txt` with this entire folder. Keep the folder intact so relative reference links work. Open `REFERENCE-GALLERY.html` to browse the images and their roles.

Read in order:
1. `CODEX_NEXT.md` — controlling assignment and execution order.
2. `references/REFERENCE-MAP.md` — exactly what to take, and not take, from each picture.
3. `packets/01-UI-CONTRACT.md` — concrete screen layouts, hierarchy, controls and state.
4. `packets/02-STUDIO-AND-TOUR-WALL.md` — preserve the set, use one existing blank wall, and improve lighting/materials/cameras.
5. `packets/03-VALIDATION-AND-DELIVERY.md` — local critique, behavior checks, native performance, Git and hosted proof.
6. `packets/04-INTEGRATION-MAP.md` — actual Review20 code seams and preservation traps.

The generated demonstration is `art/GENERATED-TOUR-WALL-DEMONSTRATION.png`. It is an ORIGINAL concept image from this conversation, not a perspective-correct wall texture. `art/WALL-TEXTURE-BRIEF.txt` directs creation of a proper flat artwork plate locally. No additional reference hunt is required from Dan.

## Decisions already made
- Keep the cinematic references as the design ambition. Use the current-game mockups to preserve the real screen functions, not to justify settling for blocky cards.
- Choose ONE of the two existing blank white side walls for the mural. Do not paint over the main cabinet/workbench/logo wall or moving bay door.
- Preserve the real set, checker/vented floor and red border. The generated concept is not permission to rebuild the room as a marble lounge.
- Implement real DOM/CSS/SVG controls and live data. Never ship a flattened AI-generated interface or invent missing vehicles, routes, accounts or replay functionality.
- Work in the existing rebuild on current main, preserving newer work. Return one complete Review21; no restart, no engine migration, no spending or new hosting project.

## What is included
Four cinematic concepts, three current-game UI concepts, five original-set photo references, the generated wall demonstration, an owner screenshot and four submitted Review20 baseline images. Images have stable IDs, descriptions and source hashes. Review copies are JPEGs at original resolution; the wall demonstration is also included as its exact original PNG.

The original game assets, original review archive and full historic evidence are NOT duplicated in this director kit. See the repository and its current handoff for them. No font binaries are included.
