# 03 — Review19 proof and current deployment

## What counts as a successful return

A meaningful new driving result with a coherent entry experience, not merely passing source tests. Internally review the actual baseline/candidate turn footage and repair while the run has capacity. Automated evidence can measure motion and spot defects, but cannot certify Dan's subjective enjoyment.

Retain all failed/intermediate native samples. Keep current owner complaints explicit until the new candidate has relevant motion proof. Label human listening/driving and physical device coverage accurately.

## Local validation

- Full pinned test suite, normal build and curated demo build; historical tests retain their named version expectations. Do not rewrite v1/v2 goldens merely because new defaults use v3. Add v3 tests and update tests of intentionally changed defaults with explicit rationale.
- Full route/profile/recipe entry matrix and save/Cup/ownership isolation described in packet02.
- Integrated steering/braking/sweeper traces with actual input, applied angles, speed/yaw/path and slip/load. Use the same controller/input protocol for baseline/new; separately exercise native keyboard and a virtual standard gamepad. Virtual devices are not physical controller certification.
- Native wall-clock first race/retry for Harbor/Express, stock/equipped, 720p low and1080p standard: the retained eight configurations/two attempts each. Two additional representative cockpit attempts. Include current UI/audio/display. No simultaneous video/trace capture or stopping unrelated user jobs.
- Preserve thresholds: p95 <=20ms, p99 <=33.4ms, max active interval <=100ms, unchanged sample selection. Full non-racing timing remains. Report real host/GPU/browser and source identity; old measurements do not certify a new machine.
- Compare retained current-art baseline when needed. Do not claim earlier performance spikes were solved solely because the latest matrix passes.

## Film and small evidence

One roughly three-minute 720p film with actual game master audio: short baseline/new steering comparison at meaningful matched speed; ordinary sweeper and braking; actual input/angle labels on the diagnostic segment; a current race with rivals; cockpit/display; reverse/recovery; return to the same build. Disclose cuts and clock method. Use continuous takes for each comparison. The comparison must not covertly slow the new car or use a more capable path controller only for it.

Capture a small set of representative stills at 720/1080 if UI labels changed. Keep the current broader UI and sound bank intact. No requirement to generate a new promotional video or engine A/B when engine sources did not change.

## Git/main portability

Work from the discovered current checkout on main, preserving newer/user work. Local recoverable commits are fine; push a completed tested checkpoint rather than every broken experiment into automatic deployment. No force-push, destructive clean/reset, new repository or arbitrary branch switching.

Update the active headers of HANDOFF.md/AGENTS.md/production state and stale active README instructions. Preserve historical sections as historical. Record exact tools, commands, relative asset locations, source/data/evidence dependencies, local URL and stop command, unresolved limitations and the next-machine resume prompt. Never treat the old work-machine root as the destination path.

Track the new runtime/authoring inputs and original audit/test data needed to reproduce the current candidate, without credentials, caches, personal profiles/saves, installed tools or dependency directories. The repo currently reports public; do not change that setting. Check accidental secret inclusion without copying values to reports. No new sound-service credentials or spending are needed.

From a fresh remote checkout, retrieve required source/runtime/editable assets without copying ignored inputs from the original directory; install pinned dependencies, build and run relevant smoke/profile checks. Host tools/package/browser caches may be shared only if disclosed. This proves recovery on that host, not destination-hardware certification. Record tested source and any docs/receipt-only follow-up separately.

## Existing Vercel integration

Owner authorization covers the existing personal rebuild, not a commissioned SlingMods production site. Exact project `slingmods-three-wheel-tour-rebuild`, ID `prj_3UBN81UQim5W3obOGNHc9wpfNTDY`, team `team_PBgvwaLCZgCbOUOfCircpnmu` are evidence from this review; verify locally before using.

Main currently auto-deploys through Vercel. Metadata now identifies the right current commit, but the observed command is the ordinary `npm run build`. Inspect the existing demo-build/publication-staging contract and establish a reproducible stable output selection for Vercel. Do not hard-code a prior timestamped staging directory or publish the entire repository, evidence, editor tools or every historical public asset. Preserve true missing-file404, correct media/wasm MIME, safe cache treatment and normal root entry. No provider/plan/visibility changes or new site. Confirm auth/capability before making a change; report a precise blocker rather than pretending it happened.

Following the completed main push, verify actual deployed commit/build identifier and open the canonical ROOT in an isolated browser, not only the legacy `?scene=bay&play=demo` route. Test fresh entry → current showroom → five previews/finish → test drive with new profile → race/retry → return and a deliberate product link. Verify audiovisual startup and browser pause/gesture behavior; note network/page-transition limits. READY status or a deployment log is not gameplay acceptance. Keep the previous deploy recoverable and do not touch other projects.

## Deliverable and retrieval

Return `Astra-Review-19-Lean.zip`, target60MB/hard cap100MB, containing small source/tests/lock/config changes, summary/audit/resume, hashed manifests, complete relevant numerical evidence and the selected film/stills. Full heavy source/runtime assets stay in Git with hashes/paths; this is not a self-contained game archive. Preserve existing Review18 ZIP unchanged.

Commit the final Review19 ZIP to main as requested. Keep runtime/tested/packaging/archive-addition/deployed identities separate to avoid recursive hashes. The external receipt records ZIP SHA/size after creation; verify the remote ZIP bytes before claiming recovery. Do not rebuild it just to retrieve it. An already authorized, allowance-verified manual Actions upload of this exact existing ZIP may be used for connector download, but is optional here; no paid artifact workflow or separate packaging project.

Final response must give the local playable URL, tested hosted root URL or precise blocker, runtime/deployed SHA, Review19 path/hash, current main tip, visible driving changes and any remaining constraint. Do not tell Dan only 'done' or leave him guessing what to send Astra.
