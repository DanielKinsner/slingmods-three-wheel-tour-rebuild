# 02 — One intentional current drive, preserved history

## Unify new-play defaults

Audit actual selections in `harbor.ts`, `crew.ts`, `express.ts`, `signature/config.ts`, `signature/scene.ts`, free-test URLs, prepared demo, Chapter01/02 and Cup adapters. Do not assume changing `freshRecipe()` updates every entry.

After candidate acceptance within the run, use a single current-profile decision for newly created builds, new ordinary time trials, quick races, free drives, new chapter events and newly started Cups. Tentative ID `slingmods-sport-v3`. New entry defaults must not secretly return to v1 because of route/file ancestry. Propagate the explicit resolved profile to player simulation, relevant input controller, same-class opponents and race metadata.

Historical v1/v2 presets remain available as clearly labeled reference choices, not additional unexplained controls competing for attention. A small consistent 'Drive tune' line in build details/settings is sufficient; do not put debug telemetry over normal racing. Debug input/limit visualization belongs behind an explicit diagnostic flag and stays out of normal publication output.

## Saved recipes and career integrity

Inspect real schema/validation and implement the minimal compatible extension. Existing recipes, installed option IDs, paint, underglow color, suspension settings, unlocks, balances and purchase receipts survive. Future/corrupt data retains explicit recovery, not silent reset.

An explicitly loaded historic recipe preserves its version unless the user deliberately makes an updated copy. Offer a clear one-action 'Use current driving' upgrade/copy without discarding the original, spending credits or confusing a handling update with a product purchase. Fresh defaults must be current; do not silently reinterpret an older stored recipe as v3.

For already in-progress Cups/attempts, keep their frozen profile/rules and consistent field until completion. Do not mix versions between legs or alter historical awards. A new Cup starts under the current profile. Existing best times/ghosts, where implemented, must retain or acquire explicit historical profile provenance; incompatible versions must not silently share a comparable record table. This is a minimal integrity change, not authorization to build a new replay platform.

Removing an accessory restores the new profile's stock parameters, not a hard-coded old profile. 'Compare stock' changes product configuration only; it must not covertly change steering version. Suspension remains a documented optional setup, not a grip or steering bonus. Free recipe test driving never purchases/unlocks career items or overwrites real career state.

## AI and input integration

The current rival and recovery logic uses `steeringLimit()` to turn requested path curvature into normalized input. Reconcile that mapping with the candidate; do not change the player's limit while leaving rivals divided by an obsolete value. Preserve real shared dynamics, visible overtaking/recovery, valid gates and race results. Adjust appropriate path lookahead/target pace only when the changed response warrants it; no speed/position teleport, invulnerable rivals or fake finishes.

Check steering-wheel geometry against applied road angles in cockpit and external views, including unwinding and reverse. Any revised presentation ratio is documented and reasonable, not a cosmetic cure. Preserve the powered display, telemetry-based RPM/audio, driver connection, and underglow/product mounting through the full maneuver set.

Add migration tests and route-by-route resolved-profile assertions, plus actual fresh/returning-save paths. Test free configure → drive → return, existing owned/removed accessories, complete chapter/Cup, finish-before-rivals pause/resume, retry, duplicate awards and prepared-demo isolation.

## Keep the product direction

No new cards, product inventory, destination, vehicle, chapter or showroom overhaul. Do not remove the existing UI/audio to get frame time. The current five supported products remain freely previewable from the beginning, career ownership remains earned, and external product links stay deliberate. No additional recordings or credentials are a prerequisite for this driving pass.
