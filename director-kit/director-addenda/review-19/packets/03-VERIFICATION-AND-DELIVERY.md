# 03 — Prove the mountain and deliver it without another giant handoff

## Required integrated evidence

1. **Physical elevation:** derived route length/gradient/elevation profile; at least ascent, crest and descent contact traces. Road render/collider agreement at sampled stations; complete grids, gates, shoulders and recovery locations use the same height. Inspect ordinary left/right turns on grades, braking, reverse, crest approach and off-road upright recovery with the actual simulated car.
2. **Preservation:** retain historical recipes/attempts/Cups and all old route records/credits. Run the full pinned suite and existing v3 pad/steering/braking regressions. Compare old flat-route behavior with the preserved Review19 runtime under matched input; do not rewrite goldens to bless unexplained drift. Repairs should be additive/bounded with explicit provenance.
3. **Playable loop:** all three new career events, unlock, single reward commits, retry, pause/finish-wait/resume, reload, selected-build returns and blocked/corrupt-storage behavior. Free test/quick race works before career unlock and cannot buy parts or advance career. Existing Harbor/Express and all five preview product links still work.
4. **Runtime visual review:** grounded trees/terrain/rails, readable car/rivals/turns, coherent district transitions, no black holes or texture repetition exposed by driving. Show every district at normal camera height in late afternoon and blue hour, including cockpit/crest views. One still per district per preset is normally enough.
5. **Performance:** no capture/tracing/archive compression during measurement. New route: two consecutive complete races for stock/equipped at 720p low and1080p standard, covering both lighting presets; at least one full cockpit race. Retain the previous p95<=20ms / p99<=33.4ms / max-active<=100ms acceptance and unfiltered phase data with declared numerical tolerance. Add old-Harbor and old-Express smoke/repeat runs to catch residency/lifecycle regressions. Warm/cold loading measurements are separate from racing FPS. State host/browser/audio/profile identity. Optimize new scene assets/chunks/shadows before cutting existing vehicle quality or silently weakening thresholds.
6. **Lifecycle:** repeated garage→mountain→Harbor→garage transitions, cancellation during load, restart, audio enable/mute/pause, display and selected-build restoration. Record resource counts as counts, not measured GPU memory. No retaining several whole destination worlds accidentally.

## Iteration policy

Run road proof, art proof, rollout, chapter integration and final critique/repair inside one substantial local assignment. Do not return after only the first two steps. Keep the best functioning candidate and all evidence. If an actual hard blocker remains after bounded repair, report the precise cause and resume state; do not pretend a flat reskin is a mountainous destination or hand Dan a list of routine tests.

## Review20 lean archive

Target60MB; hard cap **100,000,000 bytes**. Include current small source, changed authoring/export/integration scripts, tests/config/lockfile, summary/known gaps, manifests, useful stills and one roughly3–4minute720p film with actual game audio. Show a continuous representative new-route lap, a short showroom/destination selection/return and results. No cinematic-only route tour in lieu of racing. Label control automation and cuts; do not speed up playback to simulate faster rendering.

Review19 was91.46MB: valid but above target. Do not automatically include every historical exploratory telemetry file again. Keep all failed/full-resolution/raw evidence in Git with exact paths and hashes, and include all final relevant raw timing/motion plus any failures materially needed to evaluate the current candidate. Lossless solid compression and deduplication may preserve complete datasets; use a documented standard format. No outcome-only screenshots, summary-only timing or cherry-picked clean attempt. Explicitly inventory omitted bytes and their tested remote retrieval.

Keep the full terrain/road/forest authoring sources, dependency textures, licenses and runtime binaries on Git, not in the small upload unless essential to review. Do not delete prior evidence/models/ZIPs to make the packet smaller. This upload is still not a standalone game.

## Main, deployment and cross-machine recovery

Work in the current main-based project. Perform local recoverable work safely and push only completed tested checkpoints to the existing main integration. Preserve owner's newer changes; no force-push, visibility change, new repo/project, paid usage or modification of SlingMods.com. Existing personal project: `slingmods-three-wheel-tour-rebuild`, canonical root `https://slingmods-three-wheel-tour-rebuild.vercel.app/`. Use its existing auth/config and `npm run deploy:build` curated output, not a legacy bay link or the entire dev dist.

New destination assets should be lazy-loaded as required. Ensure the curated output includes every new dependency and excludes source/evidence/ZIP/Blender files. Verify on the actual canonical HTTPS root after main deploys: current source identity, mountain thumbnail/entry, elevation, full test drive or quick race, retry/return and a product link; no asset404s. READY metadata alone is not proof. If access fails, return the exact blocker and local playable result, not another account setup project. Do not fabricate hosting completion.

Update existing handoff/current state with the final implementation/runtime, tested source, deployment and packaging identities, required asset inventory, commands and exact next action. Clone from the remote in an empty location (not a copy/worktree); install pinned inputs and build/test current state without copying missing files from the authoring machine. Reused host tools/caches must be disclosed; same-host clone recovery is not a test on Dan's other computer.

Track the completed `Astra-Review-20-Lean.zip` and receipt on main after creation. A receipt-only follow-up is distinct from tested runtime; do not claim a recursive archive checksum. Re-read the remote archive metadata/checksum to prove it transferred. Optional existing artifact delivery can be reused, but do not introduce another service or workflow detour merely for this return.

## Final message to Dan

Give one short handoff: what became playable, current root URL, exact deployed/runtime/main identities, local launch command, review ZIP location/size/hash, playable film and any precise blockers. No routine QA homework or request to choose day-to-day design details. The next review should be of a new place to drive, not a plan to begin it.
