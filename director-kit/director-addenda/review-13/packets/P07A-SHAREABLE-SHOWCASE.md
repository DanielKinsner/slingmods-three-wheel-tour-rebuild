# P07A — Shareable Showcase: local candidate, not publication

## Outcome

Dan wants a small development demo he can show people soon. The next work should make the existing game understandable, recognizably SlingMods, and resilient for a first-time visitor. It must not become another indefinite scenery assignment. Keep the P06C world, repaired hero/rear/driver, physical route, actual rivals, honest product behavior and career integrity.

The target experience is: open the page, understand the controls, admire the car in the garage, enter a real race with three rivals without grinding an introduction, finish, retry, return to the garage, compare stock/underglow and deliberately open its real product listing. A daylight drive remains available so the demo does not conceal its unfinished environment with darkness. Existing career play is still accessible and unchanged.

This authorizes local implementation and validation only. Produce a deployable candidate and hosting plan; do not actually upload or alter a Vercel project, Git remote, production deployment, domain, storefront or account. A localhost URL is not a hosted/shareable URL. Do not use a tunnel as an unauthorized substitute.

## A. Recover and preserve the actual starting point

Record checkout root, branch, runtime and packaging identities, status and latest relevant commits. Preserve `P06C-HOME-KICKOFF.md` and any other unrelated user changes. Review13's own latest commits may be local-only. No reset, clean, new scaffold or replacement repository. Find tools rather than assuming either historical workstation path.

Retain source/evidence immutable snapshots. Compare protected files against the actual beginning of P07A, not a convenient historical snapshot. Older line-ending differences are not proof of changed logic; distinguish strict binary hashes from explicitly declared text normalization. Do not rewrite evidence to make it match.

Freeze physical equations, wheel/contact layout, route/collider geometry, rear and driver geometry, race/checkpoint/reward semantics, existing ownership and career save schema. Permitted seams are presentation/loading, explicit demo-profile storage, UI/input lifecycle, branding/catalog display, bounded rival recovery corrections, and a measured performance repair. Any necessary exception gets a written before/after contract and targeted regression proof. This is not permission to retune traction or engine power.

## B. Resolve or characterize the repeated-race HOLD first

Known result: Review13's equipped1080 repeat context has a passing first race and second-race p95=33.3ms against <=20ms; a fresh same-build follow-up repeats it. No active sample exceeded100ms. Pooled percentiles obscure the failure and cannot approve it. Retain complete first-matrix data, follow-up, every phase/outlier and all unsuccessful experiments.

Start with the exact frozen Review13 runtime. On this same home machine, run a naturally quiet, unrecorded two-race/retry sequence using standard quality, DPR 1, actual 1080 buffer, audio graph enabled and ordinary virtual input. Record time-aligned process/global CPU telemetry with declared sampling interval and overhead; aggregate process identities and load, not unrelated command-line contents, documents or personal paths. Do not kill/suspend unrelated processes, change power settings, alter driver caches or manipulate the active desktop. Do not benchmark concurrently with your own Blender/test/media jobs.

Compare complete attempts, startup, race progress, simulation ticks, frame/CPU-submission time, and resource trends. A naturally quiet repeat pass supports 'not reproduced under these observed conditions,' not a proven causal diagnosis. Seek two complete repeat contexts if affordable. If the issue persists, isolate with bounded diagnostic experiments: lifecycle/retry state, retained render resources, light preparation, material/shader variants, masked foliage overdraw and submission load. Audio-off/instrumentation-off/lower-resolution trials may be useful comparisons but are not substitutes for the acceptance configuration. GPU time requires actual GPU instrumentation; CPU submission time is not GPU time.

Make the smallest evidence-supported repair, document why, and rerun the affected scenarios before continuing. Do not silently reduce quality, hide heavy route sections, slow the physics or alter the scoring thresholds. The historical ready/startup spikes and shader-count transitions are separate open observations; do not claim they are solved merely because the final race runs well.

If the host remains busy, use a bounded observation/retry period rather than waiting indefinitely. Proceed with non-benchmark local work without claiming quiet-host success. After two failed repair approaches, retain the best checkpoint, precise findings and next experiment. An unresolved repeat slowdown blocks SHARE-READY, not safe progress on local UI and packaging.

## C. Demo entry and coherent flow

Add an explicit, polished 'Development Preview' entry to the existing product, not a disconnected promotional site. Primary action: Race the Harbor. Secondary actions: Explore the Garage, Daylight Drive, Continue Career. Use the existing crew event and existing solo day route, not another track or controller.

Demo mode may start with the existing SM-133 cosmetic kit available and a prepared race state. Store this in a separate versioned demo namespace or an in-memory demo store. Never import demo credits, ownership, results, leaderboard records or chapter unlocks into career. Switching mode, reloading, opening a second tab and resetting the demo must not alter personal saves. No reward bypass disguised as earned progress. Main career keeps its existing progression and rules.

Keep a short, readable controls card visible before starting: steering, throttle/brake, pause, camera and recovery, drawn from the actual bindings. Native keyboard operation must work without hidden test hooks. Explain gamepad support only to the extent tested; virtual-pad evidence is not physical-controller approval. On narrow/touch-only screens, show an honest desktop-first support note instead of claiming mobile play. Do not implement an entire mobile-control system this pass.

Results need useful next actions: Race Again, Garage, and a nonintrusive Shop This Build route. A visitor should never face a dead end or a missing feature exposed as a finished menu item. Finish positions and rival performance must come from real simulation, never a predetermined podium for the trailer.

## D. Branding and site connection

Use actual approved SlingMods logo artwork found in the existing authorized assets or verified first-party resources. Preserve its proportions, transparency and palette. Do not ask an image model to reconstruct the logo or type an approximation and call it the official wordmark. Retain source, authorization basis and file hash. If no suitable artwork is available, finish the other work and state the exact missing asset; do not fabricate one or spend money.

Compose a small, deliberate placement set: the garage hero wall, start/finish gantry, one or two strong service-frontage signs, and a restrained result/landing treatment. Use neutral illumination, charcoal/white/red brand accents, plausible physical sign scale and readable angles. Do not cover the water view or safety/braking markers. Reuse materials/geometry; signage should not require new shadow-casting lights. Prefer updating existing sign surfaces to adding another prop population. Inspect matched day and night views after each placement.

Extend the existing product link into a tidy 'Shop This Build' detail. The represented item remains TricLED's SM-133 base RGB kit. Current first-party listing title and fitment are in the technical sources. Distinguish installed, previewed and not-installed state. A longer official title may sit in the detail panel while a concise display name sits in the HUD. Show the manufacturer, part number, modeled vehicle and cosmetic-only effect; distinguish game credits from real money. Keep optional add-ons clearly excluded.

Open the verified HTTPS SlingMods product page only after deliberate user action, using normal safe external-link behavior. Confirm it resolves to the intended item rather than treating a generic HTTP200 as sufficient. The game must function without the store being online. Opening/returning from the store must not leave stuck throttle or duplicate audio. No live prices, checkout/cart mutation, account requirement, new backend, user tracking or unsupported fitment claim. Do not add purchaseable unmodeled parts merely to fill the panel.

## E. Rivals and hardening

There are already three physical rivals. Preserve their shared-world control and distinct identities. Validate grid/start separation, close encounters, actual changes in order, completing both laps, results, restart and return. A documented blocked/recovery scenario should recover without endless collisions or rewarding invalid laps. Use actual relative race progress to report passes; an AI intent counter is not proof of an overtake.

Only correct demonstrated AI lifecycle/stuck/recovery problems. Do not create a second AI stack, spline-pin their motion, teleport for cosmetic overtakes or silently equalize their engines. No rubber-band speed boost just to keep all three in every screenshot.

Cover asset/network failure with a readable retry path; clean initial loading progress; disabled WebGL or lost context with honest recovery; pause/focus/device loss with neutral input; audio permission/mute/resume without duplicate graphs; storage unavailable/corrupt/migration failure without silently deleting existing career; rapid retry/garage switching; reload/deep-link entry; invalid laps and one-time rewards. Test normal UI events, not only internal helper calls. Keep developer controls, fixture URLs and old asset selectors out of the demo build's ordinary surface. Production error reporting must not expose secrets or local filesystem paths.

## F. Smaller deployable runtime, separately from the smaller review ZIP

Static inspection of Review13 found `loadShowcaseBay()` loading the full 28,974,372-byte waterfront kit and then discarding non-bay modules. The active vehicle is 20,520,076 bytes and driver 3,717,076 bytes. These are file sizes, not measured network transfer or startup time. Measure actual cold and warm loading before claiming a bottleneck solved.

Derive a garage-only GLB from the exact existing bay nodes/materials, preserving geometry, transforms and finish. Keep editable source/export script and comparison tests; do not rebuild the room artistically. Delay course assets until a visitor chooses a drive. Prevent duplicate live renderers/audio graphs. Measure before/after byte closure and timing at stated network/host settings. No global texture downscale or new compression dependency without evidence and matched visual verification.

The existing public tree totals 233,739,495 raw bytes in the uploaded snapshot, including historical assets. Vite copies public files into its output, including unreferenced ones [T1]. Build a clean demo staging/output with an explicit tested asset allowlist, not by deleting authoring/history from the repository. Keep the normal development build and fixtures working separately. Trace runtime fetches and embedded/imported dependencies through garage, day, crew, product, retry and reload. Hidden legacy harbor geometry may still supply collision: do not remove its GLB unless an exact collision-preserving extraction and parity/clearance proof demonstrate equivalence. This extraction is optional, not a new mandatory modeling project.

Inventory final output paths/sizes/hashes; retain necessary notices, no source maps exposing private source unless deliberately approved, no research captures, evidence, original scans, Blender sources, keys or historical prototype models. Confirm missing models/maps/WASM fail the test. Test the exact staged output with a static server, not only Vite dev. Use cache policies consistent with filename versioning; never label mutable unversioned assets immutable and create mixed-build caching.

Prepare `DEPLOYMENT-READINESS.md` with exact output, build command, Node/runtime requirements, byte budget, tested routes, asset failures, base-path assumption and a proposed isolated hosting target. No project/account identifiers should be guessed. Read existing local linkage when present; do not link accounts or modify projects. Vercel's current docs explicitly say a new project's first deployment is production even without `--prod` [T2]; therefore 'just run vercel' is not a safe authorization shortcut. `.vercelignore` alone also does not prove the final published asset closure [T3]. Actual deployment and external-browser smoke testing are a later authorized operation.

## G. Final validation and one return

Run the full pinned installed suite and production/demo builds. Extend focused tests for demo/career isolation, product state/link behavior, safe loading, asset closure, input/audio lifecycle, actual retries and any scoped repair. Never weaken tests just to obtain green output. Independently inspect the resulting source, actual runtime images and ordinary motion; disclose when only a separate review pass rather than another agent was available.

Retain the established five configurations/six full crew races and a daylight lap on the FINAL runtime. Every attempt must individually meet p95<=20ms, p99<=33.4ms and no active interval>100ms, with only the existing floating-point tolerance. Include loading/result/retry phases and outliers separately. Capture media in a different run. Baseline/failure, repaired and final measurements must remain distinguishable. Report resolution/DPR, quality, actual renderer/host/browser, audio state, run conditions and validity. No universal 60 FPS, physical-controller, human fun or listening certification.

Internal checkpoints are performance disposition; demo/store/brand proof; final integrated validation; packaging. Review and repair inside the local run rather than sending Dan every intermediate checkpoint. Track independent demo readiness and the still-pending global G3/G4/final art gates. The broader visual aspiration remains unchanged.

Deliver one lean Review14 following the companion contract, plus a short completion message with verdict, local demo launch instructions, exact artifact size and any true blocker. No automated next milestone after delivery.
