# P07B — Put the existing demo on a link

## Outcome

A clearly labeled desktop Development Preview that Dan can send to a few people: open the link, understand controls, race the three existing rivals, finish/retry, inspect the SlingMods build, and intentionally open the real product page. This is delivery of the current demo, not a new content milestone.

Local acceptance is already sufficient to attempt this bounded demonstration. Keep broader art quality, physical gamepads/phones, authentic audio and G3/G4 separate. Do not make a global “production ready” claim.

## A. Resume and local demo first

Discover paths and versions. Confirm current Git status and preserve user edits, including the pre-existing P06C home kickoff. Use the current full checkout, not the lean ZIP as a game distribution. Locate `demo-current.json`, the referenced output and its manifest; verify all payload bytes, notices and current identity before using them. The submitted runtime is 758c2b291a9b5bd525fe9acd1425537657fadf70; use an explained newer identity if necessary, without reverting work.

The exact retained candidate should run through `npm run demo:preview`; default is 127.0.0.1:5188. Resolve port conflicts without terminating unrelated processes. Run an HTTP probe and record the actual command, output identity, address, owned PID/log and stop instruction. Never call a local address a public share link. Do not leave fictional background-server promises when the agent cannot retain a process.

A complete recovered checkout may need the existing dependency install/demo build. Do not reinstall tools globally, re-download authored sources or re-export artwork simply to open a retained output.

## B. Minimal publication preparation

Preserve all current vehicle, driver, route/collision, simulation, rival behavior, rewards, products, saved careers, demo namespaces, lighting/materials and environment geometry. No art downscaling, new pack libraries, new framework, backend, analytics service, account wall or monetization.

Use a separately versioned **publish stage**, made reproducibly from the exact demo output. Never upload normal `dist`, `public`, repository root, `node_modules`, `.git`, private evidence, original research, authoring assets or credentials. Keep runtime-required data, original notices and the full audio bank. Missing dependencies must fail preflight, not be hidden with a catch-all HTML response.

Permitted small changes:
- Correct the local preview `.wav` MIME mapping to `audio/wav` and assert it in a focused HTTP test.
- Add static host configuration, reviewed headers, a non-indexing signal for the development demo, and concise deployment/rollback metadata. `noindex` discourages search indexing; it is **not** access control or secrecy. Do not use “private” for a link anyone can open.
- Address a reproducible launch/copy/accessibility defect only if it blocks this sharing path. Keep its diff and regression evidence. A real game-system defect requires a precise scope decision, not a surprise redesign.

Maintain base `/` on a separate origin; a subdirectory mount is not tested. Preserve query-based scene/profile navigation. Do not rewrite missing GLB/WAV/JSON/JS paths to index.html. HTML and unversioned assets revalidate; only actually hash-versioned JS/CSS receive long immutable caching. Verify JavaScript, CSS, GLB, JSON and WAV content types and `nosniff`; keep the no-referrer policy. Do not bolt on an untested restrictive CSP that breaks blob-decoded textures or physics WASM.

Inspect metadata/notices for accidental local paths, research references or secrets before public staging. Preserve legitimate attribution. Do not remove licensing information to make the output look smaller. Any unresolved third-party public-redistribution issue is a real blocker, not solved by describing the work as a demo.

Test/build the small changes with the pinned local toolchain. Record before/after source and binary hashes. Retain the original Review14 output unchanged so the candidate remains reversible.

## C. Host choice and publication boundary

**Do not infer publication approval from this packet, the Review14 upload, or an old desire to see a demo.** Use Dan's accompanying explicit host/publication instruction. Resolve existing authorized account data before asking for a username/ID. Use the provider's normal authorization flow, never ask for a token in chat or store it in reports.

### Preferred no-new-spend route: separate Netlify Free demo, after approval

Netlify's published Free-plan announcement permits commercial projects. Current Free plans use hard quotas; confirm the actual team's plan, owner and remaining quota rather than relying on a historic allowance. Inspect whether a shared quota could pause unrelated production projects. Do not change a team plan, enable paid add-ons/auto-recharge, create duplicate accounts to evade quotas, or migrate a legacy plan. Missing account/permission is one actionable owner prerequisite.

Use a dedicated project such as `slingmods-three-wheel-tour-demo` with an available suffix when necessary, selected within the approved account. Do not connect or change an existing site. Do not import/link the private source repository, install Git webhooks or create continuous production deployment. A manual prebuilt CLI/file-digest route is preferred. Confirm the installed CLI help and current official documentation before constructing a command; no guessed flags or provider APIs.

A fresh project may be owner-private until explicitly published. After Dan approves a public development-demo link, change only this new project's visibility as needed and verify a logged-out visitor can open it. No custom domain or SlingMods.com integration is required. A generated provider origin is the target. Record the actual project/deploy IDs and URL returned by the provider; do not invent a slug-derived URL as success.

For the approximately 112.46 MB current payload, verify actual upload acceptance and quotas. The 50.8 MB review ZIP is unrelated to the deployment budget. Use the provider's documented file-digest/CLI workflow rather than assuming browser drag-and-drop will accept a large asset set. Transient uploads may receive bounded retries with logs; do not rebuild or reduce geometry just because of a network timeout. Anonymous, unclaimed one-hour links are not an acceptable durable handoff.

### Vercel alternative only when separately approved and eligible

The connected team observed during this review was Hobby. It is not a default eligible destination for a company-branded retail-linked demo. Official current docs also limit CLI source uploads to100 MB on Hobby, versus1 GB on Pro, and describe a new project's first deployment as production even without --prod. Do not try to bypass these with a filename or a different flag.

If Dan selects an existing commercial-capable team or explicitly authorizes a plan change, inspect it first. No payment is authorized in the default packet. Use a separate static project/origin, never the old game's production project, YES products, or a storefront domain. Convert the selected artifact into the documented static deployment format and verify exact routes/headers and external behavior. Do not assume ordinary `vercel deploy` merely stages an unpublished preview.

## D. Hosted acceptance — test the real address

Run this as one autonomous review/repair sequence after the authorized upload; do not ask Dan to be the test runner.

1. **HTTP/output identity:** request the returned HTTPS origin in a clean context; verify root, build identity, all payload hashes after transfer decoding, content types and representative cache headers. Keep any intentional provider metadata transformations explicit. Verify missing assets return404, not200 HTML. Ensure no private source/evidence directories are exposed. Do not include authorization cookies/tokens in exported HAR/logs.
2. **First visit:** fresh cache, ordinary URL, no test parameters or virtual input needed to see the controls. Verify desktop limitations and all four entry actions. Use ordinary keyboard/menu input to start the race. No required external service or hidden pre-earned career prerequisite.
3. **Racing loop:** one actual two-lap crew race, result, retry and a second race on the same tab, then garage. Use the existing accepted automation for the long driving portion, explicitly labeled. Confirm all rivals make progress/recover/finish under the existing rules, result/reward once, no race-start soft lock or held-key jump after menu/blur. Do not retune the AI simply to stage a flattering recording.
4. **Build/site path:** change a demo kit setting, compare stock, move between garage/daylight/race, and verify demo state stays separate from a seeded isolated test career. Confirm the intentional Shop This Build link and safe return to the game. Do not purchase a real product, touch personal browser storage or mutate the storefront.
5. **Recovery:** keyboard pause/resume, interrupted reset hold, navigation/reload/back, denied storage and a deliberately blocked required asset. Reuse the existing tests, target the actual HTTPS origin, and preserve raw failures. Ensure clear recovery rather than an empty scene or infinite load. Touch-only access must retain its honest support message; do not claim phone support.
6. **Timing:** one clean native 1080p equipped two-race repeat on the known local GPU, separate from the recording. Keep both complete active attempts, loading phases, interval outliers, runtime/hardware identity and overflow flags. Report p95/p99 per attempt, not pooled. Do not require another full scenery/performance matrix when the game is unchanged. If the old slowdown recurs, reproduce once with context and report it honestly; do not label a cause without evidence or claim universal smoothness. A software-rendered container run is functional evidence only, not the RTX4080 benchmark.

The useful pass is **SHAREABLE DEVELOPMENT PREVIEW**: reachable by the intended recipients, real game loop works, provenance/known limits disclosed. Do not issue G3/G4 or final-release approval. Failure handling is bounded and preserves the best candidate. If public access or hosting cannot be resolved, return the exact constraint and retained local demo, not another giant archive or unrelated feature work.

## E. One small return

The main deliverable is the **actual tested URL**, not a new beauty-review ZIP. Include a short note for Dan: supported controls/browser, demo-versus-career behavior, what works and the few known caveats. Preserve recovery/rollback instructions for this dedicated demo only.

If an evidence upload is needed, return **Astra-Review-15-Hosting.zip**, target ≤20 MB, hard cap60 MB. Include:
- exact source/output/deployment identities, manifest, plan/visibility verification and omission/recovery index;
- current small code/config/scripts/tests, a short explained diff, build/focused-test logs;
- complete relevant hosted HTTP/smoke/two-race timing records, including failures/outliers;
- roughly4–6 purposeful final stills and at most one short720p clip proving hosted entry/race/garage when useful. Do not duplicate Review14's3½-minute film or every prior capture.

All bulky editable sources and original evidence remain preserved in the full project. Do not assert that LOCAL_ONLY files are remotely recoverable unless actually verified. Git push remains outside this default scope; ask only if a push is genuinely required, not as an automatic publication side effect. The manual static route should not require one.
