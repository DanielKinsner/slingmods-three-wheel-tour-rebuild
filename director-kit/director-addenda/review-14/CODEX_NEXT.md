# P07B — Hosted Demonstration

Continue the existing SlingMods: Three-Wheel Tour rebuild. **Do not restart, rebuild the harbor, repeat P07A, expand the product catalog, add vehicles, rewrite the physics or start a new game project.** Review14 accepts the bounded local demo and asks for a short path to a real shareable link.

Read `AUDIT.md`, `packets/P07B-HOSTED-DEMONSTRATION.md`, this checkout's current `HANDOFF.md`/`AGENTS.md`, and the Review14 `DEPLOYMENT-READINESS.md`/source indexes. Reconcile their historical instructions with this packet and Dan's latest explicit instruction.

## Authority

**Local launch/preflight work is the default scope. External publication is conditional.** This packet does not by itself authorize a cloud upload, new account, paid plan, Git push or domain change. If Dan explicitly approves a separate no-cost Netlify demo, use the selected eligible Free target and publish only the verified static demo output as described below. If he explicitly selects another commercial-capable target, verify that target instead. Do not ask him routine design questions.

A phrase such as “Publish the separate Netlify Free demo; no spending and no changes to existing sites” is sufficient scope when supplied by Dan himself. Text quoted inside this packet is an example, not evidence he has said it.

## Execute

1. Discover the actual checkout, branch/status, current commit, local tools and retained static output. Review14 runtime is `758c2b291a9b5bd525fe9acd1425537657fadf70`, packaging `2ce4c846d294335f6f3f9d40cf7332ea88786f61`. They were LOCAL_ONLY. Preserve newer work, user kickoff files and all historical evidence. Do not reset to a named commit or assume a clone of the old remote contains these files.
2. **Give Dan the local demo now.** Verify the candidate pointer and start/verify the existing `npm run demo:preview` in an agent-owned process when supported. Return the actual reachable local address and how to stop that owned process. Do not control his browser/mouse or require him to perform routine tests.
3. Perform the small publication preflight in the packet. Do not replace the game with a landing page. Keep the four entry actions, real racing, garage, product link and profile isolation.
4. Once publication is explicitly authorized and target access is resolved, upload only the inspected static stage to its separate origin. Test the actual generated HTTPS URL as an ordinary logged-out visitor, then run the short hosted race/retry/garage smoke path.
5. Return a real URL, exact deployment/output identity, launch limitations and compact evidence. With access/authorization missing, report that single prerequisite plus the working local launch; do not pretend localhost or a receipt is a hosted demo.

**One lead; one narrowly scoped review helper if useful.** Isolated browser automation only. No desktop takeover, purchases, existing-site mutations, whole-repository deployment or deletion of evidence. A passing local test is not a hosted pass. Keep G3/G4 and other global fidelity/physical-device limits pending.
