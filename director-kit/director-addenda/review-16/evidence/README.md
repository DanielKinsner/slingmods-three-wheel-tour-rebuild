Independent read-only Review16 inspection records. These files are evidence, not game modifications. The source ZIP and exact remote commit retain complete raw records. The scoped test harness used a resolver for two omitted original JSON inputs; initial missing-input failures are retained. The final86-test pass is `independent-tests-final-02.log`. Native/frame/driving data in `independent-measurements.json` were recalculated from submitted raw samples; Astra did not run the full game or hardware benchmark. Still/frame sheets are sampled original captures, not new renders. Source-generation and capture settings belong to the original submitted reports.
