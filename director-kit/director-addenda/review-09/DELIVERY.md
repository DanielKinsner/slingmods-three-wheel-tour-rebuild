# One final handoff: Astra-Review-10.zip

Package only after the integrated chapter and internal review, or an actual unresolved blocker/session constraint. Intermediate source commits, captures and repair notes remain local. Do not ask Dan to send I1, I2 and I3 as separate ZIPs.

## Required contents

`REVIEW-ME-FIRST.md`: exact root/runtime/packaging commits, launch command and ordinary root-page flow; what a fresh and an existing career can do; all completed/held acceptance IDs; controls; real limitations; whether physical controller/human audio/foreground performance were unavailable. One page of meaningful status, not a congratulatory checklist.

Full current `src/`, tests, relevant scripts, package/lock/build configuration, assets required to run and current chapter definitions. Preserve relative paths. Include the new/changed Blender generation sources and metadata; unchanged large `.blend` binaries need not be duplicated in the review archive, but retain and identify them in the actual repo. Do not export dependencies, `.git`, caches, tools, private profiles, secrets, `.env`, historical redundant evidence or current user's browser data.

Include the active P05 instructions, current ledger excerpt (clearly labeled as an excerpt), milestone/reviewer notes, baseline/after parity outputs, actual tests/build outputs, AI/race adversarial results, save-migration/idempotency checks and raw clean performance profiles. Bind every capture/build result to input hashes and exact configuration. A stale video is not current evidence because its filename says final.

## Compact evidence selection

- Six to eight representative runtime stills: chapter/bay, full starting grid, nearby contest for position, cockpit with a rival, final-lap HUD, valid result with reward breakdown, returned saved build, and an honest failure/retry or day time-trial regression view. No full-resolution duplicate image archive.
- One continuous **ordinary wall-clock** complete two-lap crew-race video, preferably with synchronized game audio, including grid/countdown and result. Use the existing virtual player-input harness only with an explicit label; all rivals use production controllers in the same world. Show a normal near/far/cockpit change and a look-back where safe. Do not fake a pass for the recording, hand-set a winner, splice out a stall or replace runtime audio with an unrelated soundtrack. If sound capture is unavailable, mark SILENT prominently and retain separate audio/mixer evidence rather than claiming listening approval.
- One short, clearly edited-and-labeled flow demonstration of opening chapter, result/rewards, garage, actual kit preservation/reload and retry, only if the continuous race does not communicate these parts. Avoid adding another long redundant lap video.
- At least two physics-driven full field runs with different seeds/start-order fixtures for QA, with raw telemetry and normal render evidence. Race contact/blocked-path recovery may be separate explicitly staged tests; normal gameplay must not include unexplained resets.

Clean performance profiles are **separate from the videos**, recorded without screen recording/readback/full inspection. First grid/race, same-context retry and next scene entry are represented. At least stock/equipped 1080p and 720p cases and a repeat of the heaviest setup; preserve outliers, loading times, hardware/browser/drawing-buffer/quality flags and active participant counts. Automated controlled time is suitable for deterministic physics/rule tests, not frame-rate proof.

Target a compact review ZIP (roughly 100–150 MB or less where practical), but never omit necessary runtime assets or decisive raw evidence to hit a cosmetic size limit. Do not include every failed iteration. Create a numbered filename if a current review ZIP already exists; do not destroy it. Report exact path and size. No deployment or Library upload without a request.

## On session limits or hard blockers

Persist `AUTONOMOUS_RESUME.md` inside the repo and include it if interrupted: last known-good commit, completed/remaining acceptance IDs, reproduced blocker, next exact command, modified paths and what NOT to redo. Preserve working pieces; do not restart or strip out collisions/validation to claim completion. A saved continuation is not a promise of automatic background work.
