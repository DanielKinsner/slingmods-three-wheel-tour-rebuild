# Sources and evidence boundary

## User-provided project sources

This review is primarily based on the uploaded `Astra-Review-04.zip`, its actual source/GLB/PNG/MP4 files, and byte comparison against `Astra-Review-03.zip`. Search returned no indexed archive text; the already mounted ZIP was safely extracted for programmatic inspection.

Independent reviewer outputs are in `evidence/`. The 16 native tests are actual executions of the submitted dependency-free modules, with only test import extension adaptation. Full browser/Blender/game tests were not rerun here. The scalar look-back equation replay is explicitly not a full THREE camera execution.

## Primary implementation references checked September 11, 2026

- MDN, OfflineAudioContext: https://developer.mozilla.org/en-US/docs/Web/API/OfflineAudioContext — renders the audio graph into a buffer instead of device hardware. Supports the proposed shared-graph offline evidence method; does not prove live playback.
- MDN, AudioContext.resume(): https://developer.mozilla.org/en-US/docs/Web/API/AudioContext/resume — resumes a suspended realtime context and returns a promise. Handle real context state and errors.
- MDN, Web Audio API best practices: https://developer.mozilla.org/en-US/docs/Web/API/Web_Audio_API/Best_practices — source/graph design, user activation and controls; check the current installed browser rather than assuming autoplay.
- ElevenLabs, Create sound effect: https://elevenlabs.io/docs/api-reference/text-to-sound-effects/convert — development-time sound generation option, only under existing verified authority/rights/budget. Endpoint availability is not permission to spend, redistribution authorization for an unspecified account, or proof that a generated engine sounds authentic.

No new physical vehicle specifications, performance gains or product fitment claims are introduced in this packet. All timing, camera, mesh, sound-level and demonstration limits are chosen production parameters or explicitly reported measurements of this code/package.
