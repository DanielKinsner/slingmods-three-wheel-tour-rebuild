# Validation and one-package delivery

## Internal acceptance cases

These are practical local checks, not reasons to ask Dan to relay another ZIP. Use the full normal build/test toolchain in the existing environment. Preserve historical evidence and mark each current case pass/hold/unavailable with concrete files and method limits.

1. **Preservation.** Accepted hero/rear/driver/product sources and exported hero remain unchanged, except an explicitly documented material-only calibration or optional derived LOD. Wheel anchors/contact geometry and physics tuning remain unchanged. Old solo telemetry regression, four-car collision tests, input/rearming, lap/rank validity, audio mapping, transaction/idempotence and v1/v2 save fixtures still pass. If a physical collision or race-rule defect is discovered, repair it narrowly with a failing-then-passing test; do not freeze a demonstrated defect merely to preserve a hash. Explain any exception.
2. **Complete playable flow.** Ordinary root → fresh introduction/solo reward → optional existing product purchase → crew race → correct result/reward → saved bay → immediate retry. Also eligible existing v2 build, unowned stock, denied storage, pending write, pause/reconnect and root-menu transitions. Do not manufacture ownership or a podium to get the happy-path video. Keep fourth-place completion and its reward valid without first-podium bonus; invalid/DNF cannot farm rewards. No duplicated awards after result reopening/reload.
3. **Course/showroom art.** Matched before/after cameras in each of four course districts, day and night, at normal driving height. A straight-through complete lap shows no old empty placeholder section concealed behind the best screenshot. The compact garage reads as a coherent designed space; neutral inspection remains usable. Runtime exports have actual texture maps/UVs and no glaring seams, black reflections, missing foliage or z-fighting. No new road/curb collision change under an art task.
4. **Lighting/readability.** 3/8/20-meter comparison behind the actual rivals, including an underlit turn, with common cameras/exposure and stock/cyan/red variants. Rival color, road edge and accessory remain readable. Do not accept identical all-white rivals just because pixel brightness increased. Keep practical-light program counts stable while traversing the circuit and switching allowed camera/quality states.
5. **Normal UI and traffic.** Required garage/product controls fit or scroll accessibly at both 720p and 1080p; nothing falls behind the footer. No development candidate text in ordinary player UI. Controller focus/back/pause/mute survives scene changes. Test following, clear pass, blocked corner and recovery with production controls. Distinguish completed passes from attempted maneuvers and record genuine order changes. No new rubberband, hidden AI grip or transform-teleport shortcuts.
6. **Actual audio evidence.** Main recording includes real captured game audio or a clearly documented separately synchronized stream. Confirm actual tracks, duration, sample rate and alignment at three events. Mute/volume/pause and stereo encounter balance work; no persistent clipping, orphan engines or growth in audio nodes over retries. Do not present numerical RMS alone as a sound-quality approval.
7. **Performance and lifecycle.** Use the method below. Build/readiness/prewarm precedes timed racing. No recorder, screenshots/readbacks, controlled clock or heavyweight inspect call inside a scored run. Resource lifetime stabilizes across race→bay→race and at least three retries. An independent recorded run exercises ordinary input/camera paths. Don't fix capture overhead by weakening actual game behavior.

## Bounded performance work, not another endless handoff

Keep Review10's original **366.6-ms** stock720 outlier and **33.4/50-ms p95/p99** equipped1080 repeat, alongside its later clean followups. Their cause is **not established**. Do not rename them solved, delete them, blame an unrelated process observed later, or endlessly repeat trials until a preferred result appears.

At baseline, reproduce one stock720 race and an equipped1080 pair with the present build. Capture all raw phases, actual buffer size/DPR/quality, device/browser identifiers, program/resource counts, and background contention only when actually observable without manipulating Dan's desktop. Leave his applications alone. Separate initial preparation from active racing.

After P06 integration, run native unrecorded four-car night racing in stock/equipped at actual 720p/1080p; repeat the equipped1080 case. Retain every attempt. Keep day solo as a functional/visual regression. Use the previously declared working targets: active-race nearest-rank **p95 <= 20ms, p99 <= 33.4ms**, with no unexplained recurring >100-ms racing pauses. Report every >100-ms interval and clipped simulation time, including a sole outlier. An isolated unexplained one remains an explicit reservation rather than permission to erase it.

If a problem recurs, do one separate tracing run near the same conditions, identify the measurable subsystem, then implement and compare a specific remedy. A trace that does not reproduce the issue does not establish cause. If clean repeats do not reproduce the historical outlier, retain that open observation and continue independent scene/UI work; do not return a profiler-only ZIP or perform speculative simulation surgery. A reproducible current severe stall or steady percentile regression does block calling P06 ready, but should normally be repaired in this run. Session limits then require a concrete resume note.

Use batching/culling/material sharing/appropriate LOD or settings selected transparently to regain headroom. Do not lower render resolution secretly, strip the cars/driver from performance runs, zero audio while claiming the game mix is active, disable valid-collision/rival behavior, inflate elapsed lap time, or shorten the route. Software-rendered runs must be labeled, not treated as the RTX4080 target. No general “60 FPS everywhere” claim.

## Final deliverable: Astra-Review-11.zip

One ZIP in the existing project root; if the name exists, create a numbered filename, never overwrite history. Include:

- `REVIEW-ME-FIRST.md`: current root/commits, exact install/run/ordinary entry, what changed and is playable, local acceptance verdicts, unresolved issues, measured performance context, audio/capture method and unverified physical-device/human limits. Distinguish old history from current build. `AUTONOMOUS_RESUME.md` only when work remains or a genuine blocker exists.
- Current `src`, `scripts`, `tests`, `public`, package/lock/config/entry files, and new/changed Blender authoring scripts plus new/changed editable `.blend` sources. Existing unchanged large originals can remain local if their exact paths and hashes are recorded. No anonymous meshes with no editable source. Include only the current relevant ledger excerpt while preserving the full ledger locally.
- One continuous ordinary-path first-chapter video with actual audio and UI, from grid through two valid laps/result and saved bay; also one brief garage/build/daylight demonstration. The same player need not podium every time. Include normal camera changes and one genuine traffic encounter. Identify virtual input versus human play. Silence or capture limitations must remain prominent if genuinely unavoidable; don't fill with a dubbed soundtrack.
- A curated matched comparison set: four course districts day/night; garage stock/installed; headlight spacing/calibration; readable grid/result/HUD. Save exact view transforms and scene settings for reproducibility. Before means the actual Review10 baseline, not a deliberately uglier render. Include new asset/material census and changed-source/asset hashes.
- Test/build logs, browser lifecycle/save evidence, raw clean performance rows, current resource counts/LOD behavior, and representative raw four-car telemetry with route/build provenance. Preserve original failed runs and list their paths; omit redundant historical movies from this ZIP rather than deleting them from the repo.
- `PACKAGE-MANIFEST.json` binding relative paths, sizes and SHA-256; runtime commit and packaging commit separate where appropriate. Exclude `.git`, node_modules, dist, downloaded tools, caches, backups, secrets and the ZIP itself. Check actual contents before delivery.

Aim for a convenient archive by curating duplicate media and compressing raw telemetry, not by stripping away the implementation needed for review. Finish with exact ZIP path, size and a compact statement of delivered/unresolved items. Do not push, deploy or ask Dan to manually playtest individual cases. This chat reviews the single integrated package after the local run; it is not silently monitoring Codex between handoffs.
